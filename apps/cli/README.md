# sentinelqa-cli

Status: `Planned` — the Typer-based `sentinel` CLI lands in (PRD §13, CLAUDE §13).

This package currently exposes only the empty `sentinel_cli` namespace so the workspace, type-checker, and import graph are in place. See `plans/`.
