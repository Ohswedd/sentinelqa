"""CLI command implementations (Phase 02).

Each command lives in its own module. Stubs for commands whose real
implementations land in later phases share :mod:`sentinel_cli.commands.stubs`.
"""

__all__: list[str] = []
