---
title: Python SDK
description: Public surface of the `sentinelqa` Python package.
status: Stable
---

<!-- DO NOT EDIT — this file is generated by scripts/docs/gen_sdk_reference.py. Regenerate with `make docs-gen-sdk`. -->

The `sentinelqa` package exposes a small, stable surface: the `Sentinel` facade (sync + async), the typed result models, the error hierarchy, and the agent-message serialiser. The public surface is locked by an API snapshot — breaking changes require an ADR + a deprecation window (Phase 16 deprecation policy).

## Quickstart

```python
from sentinelqa import Sentinel

sentinel = Sentinel.from_config_file('sentinel.config.yaml')
result = sentinel.audit()

if not result.passed:
    for blocker in result.blockers:
        print(blocker.title, blocker.recommendation)
```

Async parity is available for every public method:

```python
result = await sentinel.async_audit()
```

## Modules

The following modules constitute the public surface. The exact member list below is generated from `packages/python-sdk/api-snapshot.json`.

### `sentinelqa`

| Name | Kind |
|---|---|
| `AGENT_MESSAGE_SCHEMA_VERSION` | constant |
| `AuditResult` | class |
| `ConfigError` | class |
| `DependencyMissingError` | class |
| `Evidence` | class |
| `FINDINGS_SCHEMA_VERSION` | constant |
| `Finding` | class |
| `Flow` | class |
| `ModuleResult` | class |
| `Policy` | class |
| `QualityGate` | class |
| `QualityGateFailedError` | class |
| `REPAIR_SUGGESTION_SCHEMA_VERSION` | constant |
| `RUN_SCHEMA_VERSION` | constant |
| `RepairSuggestion` | class |
| `RiskMap` | class |
| `SCORE_SCHEMA_VERSION` | constant |
| `Sentinel` | class |
| `SentinelError` | class |
| `TestExecutionError` | class |
| `TestPlan` | class |
| `UnsafeTargetError` | class |

### `sentinelqa.agent`

| Name | Kind |
|---|---|
| `AGENT_MESSAGE_SCHEMA_VERSION` | constant |
| `Format` | _LiteralGenericAlias |
| `audit_result_to_agent_messages` | function |
| `finding_to_agent_message` | function |
| `format` | function |
| `repair_suggestion_to_agent_message` | function |

### `sentinelqa.errors`

| Name | Kind |
|---|---|
| `ConfigError` | class |
| `ConfigFileNotFoundError` | class |
| `ConfigSchemaError` | class |
| `ConfigSecretInlineError` | class |
| `DependencyMissingError` | class |
| `DestructiveWithoutProofError` | class |
| `ForbiddenFlagError` | class |
| `QualityGateFailedError` | class |
| `SentinelError` | class |
| `TestExecutionError` | class |
| `UnknownHostError` | class |
| `UnsafeTargetError` | class |
| `from_dict` | function |

### `sentinelqa.plugins`

| Name | Kind |
|---|---|
| `AuthPlugin` | class |
| `CloudExecutionPlugin` | class |
| `DataFixturePlugin` | class |
| `DiscoveryPlugin` | class |
| `ENTRY_POINT_GROUP` | constant |
| `PLUGIN_PROTOCOLS` | dict |
| `PROTOCOL_VERSION` | constant |
| `PluginContext` | class |
| `PolicyPlugin` | class |
| `ReporterPlugin` | class |
| `RunnerPlugin` | class |
| `ScannerPlugin` | class |

## Agent messages

Every public exception, every `Finding`, every `RepairSuggestion`, and `AuditResult.to_agent_messages()` produce stable, redacted dicts versioned by `AGENT_MESSAGE_SCHEMA_VERSION`. Use `sentinelqa.agent.format(messages, format='ndjson')` for a deterministic byte-stable stream.

## Stability

- Adding members never breaks; removing or renaming requires an ADR.
- The snapshot is regenerated via `make sdk-api-snapshot`.
- `tests/unit/sdk/test_api_snapshot.py` fails on every CI pass if the snapshot drifts from a fresh dump.
