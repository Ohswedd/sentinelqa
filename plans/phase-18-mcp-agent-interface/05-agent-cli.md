# Task 18.05 — `sentinel mcp` command

## Deliverables

- Replace Phase 02 stub of `mcp`.
- Options: `--stdio` (default), `--http <port>`, `--config`, `--log-level`.
- Documented usage with Claude Desktop / generic MCP client at `docs/user/mcp.md`.

## Acceptance criteria

- Command starts and is reachable by an MCP client.

## Tests required

- `tests/integration/cli/test_mcp.py`.

## PRD / CLAUDE.md references

- PRD §13.
- CLAUDE.md §13, §15.

## Definition of Done

- [ ] CLI command working.
- [ ] User docs committed.
- [ ] `STATUS.md` updated.
