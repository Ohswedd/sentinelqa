# Task 12.06 — `sentinel perf` command

## Deliverables

- Replace Phase 02 stub; options: `--url`, `--config`, `--samples N`, `--routes`, `--ci`, `--json`.
- Outputs `perf/<route>.json` per route plus a module result.

## Tests required

- `tests/integration/cli/test_perf.py`.

## Definition of Done

- [ ] CLI command working.
- [ ] Output labels metrics "synthetic".
- [ ] `STATUS.md` updated.
