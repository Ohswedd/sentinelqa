# SentinelQA — Live Status

Update this file on every commit that advances or completes work. Do not advance the phase pointer past unfinished items.

## Active pointer

- **Phase:** 23 — Chaos Module
- **Sub-phase:** _to be determined from `plans/phase-23-chaos-module/README.md`_
- **Active task:** first task in `plans/phase-23-chaos-module/`.
- **Branch:** to be created on next phase start; Phase 22 branch deleted on merge.
- **Blockers:** none. Phase 22 closed today. `make ci` green (2492 Python + 236 TS = 2728), `make coverage` 95.04 % (floor 95 %). `modules/api` per-file: `__init__.py` / `checks/contract_graphql.py` / `checks/latency.py` / `http_client.py` / `models.py` / `options.py` 100 %; `checks/auth.py` 98 %; `module.py` 97 %; `graphql.py` 97 %; `findings.py` 95 %; `backward_compat.py` 94 %; `checks/contract_openapi.py` 94 %; `checks/negative.py` 92 %; `checks/pagination.py` 92 %; `openapi.py` 92 %; `checks/error_shape.py` 91 %. `apps/cli/.../api_cmd.py` covered via integration tests. ADR-0027 accepted. PRD §10.3.1 expanded.
- **Last updated:** 2026-05-29 by ohswedd

---

## Phase progress

Legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

- [x] Phase 00 — Foundation
- [x] Phase 01 — Core Domain & Config
- [x] Phase 02 — CLI Skeleton & Run Lifecycle
- [x] Phase 03 — Report Schemas & Reporter
- [x] Phase 04 — TypeScript Playwright Runtime
- [x] Phase 05 — Discovery Module
- [x] Phase 06 — Planner Module
- [x] Phase 07 — Generator Module
- [x] Phase 08 — Runner Module
- [x] Phase 09 — Analyzer Module
- [x] Phase 10 — Functional Module
- [x] Phase 11 — Accessibility Module
- [x] Phase 12 — Performance Module
- [x] Phase 13 — Security (Safe) Module
- [x] Phase 14 — Quality Scoring
- [x] Phase 15 — HTML & JSON Reports
- [x] Phase 16 — Python SDK
- [x] Phase 17 — CI Integration
- [x] Phase 18 — MCP & Agent Interface
- [x] Phase 19 — LLM-Code Audit Module
- [x] Phase 20 — Healer / Self-Repair
- [x] Phase 21 — Visual Regression
- [x] Phase 22 — API Testing
- [ ] Phase 23 — Chaos Module
- [ ] Phase 24 — Plugin Architecture
- [ ] Phase 25 — Integrations
- [ ] Phase 26 — Example Apps
- [ ] Phase 27 — Docs & ADRs
- [ ] Phase 28 — Versioning & Release Prep
- [ ] Phase 29 — Final Hardening & PRD Reconciliation

---

## Phase Gate Reviews

For each phase, record the gate review verdict and the reviewer's signature once the phase is complete. A phase is **not** considered done until its gate row is filled in here.

| Phase | Status | Gate verdict | Reviewer | Date | Notes / linked commit |
|---|---|---|---|---|---|
| 00 | done | PASS (CI-on-remote verification closed during Phase 01 — see Notes) | ohswedd | 2026-05-27 | All nine tasks (00.01–00.09) shipped on branch `feature/phase-00-foundation`. `make ci` green: ruff format-check + ruff lint + mypy strict + adr-check + pytest (4 tests) + Prettier + ESLint (3 workspaces) + tsc + Vitest (2 tests). pre-commit (gitleaks + commitlint + pre-push make-ci) wired and probed. ADRs 0001–0004 accepted. Repo tree matches PRD §11.2 exactly (41 directories). 0 AI co-author trailers in 11 commits. gitleaks: 0 leaks across 739 KB of history. **Verification gap closed (2026-05-27):** the private GitHub repo `Ohswedd/sentinelqa` was created at the end of Phase 01; PR #1 (Phase 01 → main) exercised all 8 required checks. The first run surfaced two genuine Phase-00 workflow bugs — `gitleaks-action@v2` crashed when trying to upload a SARIF artifact that was never written (no leaks), and `lychee --base .` failed because lychee 0.23 requires an absolute path or URL. Both fixed in commit `3148fd4`; all 8 checks green afterward. Branch protection on `main` could not be wired via API because GitHub gates that feature behind GitHub Pro for private repos; the user-facing equivalent (pre-push hook + CODEOWNERS + Conventional Commits in CI) is in place locally and on the remote checks. |
| 01 | done | PASS | ohswedd | 2026-05-27 | All eight tasks (01.01–01.08) shipped on branch `feature/phase-01-core-domain-config`. `make ci` green: ruff format-check + ruff lint + mypy strict + adr-check + pytest (193 tests with `make test-full`, 187 with default markers) + Prettier + ESLint + tsc + Vitest. `make coverage` reports 96.24% (floor: 95%). Domain models: every PRD §18.1 entity is a frozen Pydantic v2 model with prefix-based ID generator, `extra="forbid"`, and SCHEMA_VERSION ClassVar; `make schemas` writes 17 stable `*.schema.json` files into `packages/shared-schema/schemas/`. Config: strict `sentinel.config.yaml` loader with `${ENV}` interpolation, unknown-key rejection, inline-secret refusal; `sentinel.config.yaml.example` round-trips. Safety: `SafetyPolicy.enforce` covers local/allowlisted/destructive branches; `UnsafeTargetError` → exit 4; audit log writes redacted JSONL; forbidden-feature deny-list scanned by `tests/security/test_no_stealth_flags.py`. Errors: typed hierarchy (`SentinelError` → 11 subclasses), `to_agent_message()` redacted; exit codes 0/1/2/3/4/5/6/7 wired through `engine.errors.codes.ERROR_REGISTRY`. Redaction: real impl, header + URL + dict surfaces, entropy heuristic + hypothesis property tests (10 000 examples slow tier). Logging: human/JSON/quiet modes, redaction filter on every record, context-var enrichment. ADR-0005 (Config schema), ADR-0006 (Safety policy), `docs/dev/schema-versioning.md`, and `docs/user/error-codes.md` shipped. PRD §13.2 was corrected to the canonical 8-code exit grid in the same branch (sync log entry below). 0 AI co-author trailers. |
| 02 | done | PASS | ohswedd | 2026-05-27 | All eight tasks (02.01–02.08) shipped on branch `feature/phase-02-cli-skeleton-run-lifecycle`. `sentinel --help` lists every PRD §13.1 command (init/doctor/audit implemented; the other 16 are registered stubs that exit 7 with a "lands in Phase NN" message — CLAUDE §37, no fake completion). `sentinel --version` returns the pyproject version. `sentinel init` is idempotent, `--force`-safe, and the bundled `.github/workflows/sentinel.yml` is byte-equal to PRD §21.1. `sentinel doctor` runs Python/Node/Playwright/config/safety/reachability/env-var/`.sentinel`-writable/disk checks with human ASCII or single-line JSON output. The canonical 17-step run lifecycle lives at `engine/orchestrator/run_lifecycle.py`: safety policy is enforced exactly once (unsafe → exit 4 with audit.log + minimal run.json), `--dry-run` stops after `build_execution_plan` with `status="dry_run"`, module errors mark the run `incomplete` (exit 6) without crashing the run, `--ci` forces JSON mode + no prompts. Artifact tree per CLAUDE §11 with atomic writes (write → fsync → rename), retention helper (`prune_old_runs`, pinned-run support), POSIX symlink/Windows marker for `latest`. JSON-mode purity proven by a `SENTINELQA_ASSERT_JSON_STDOUT=1` write-guard that fails fast on non-JSON stdout. CLI exit-code grid (0–7) fully reachable in tests via `CliRunner`. Engine reorganized as a workspace member (`engine/pyproject.toml`) so `pip install -e apps/cli` brings in `sentinelqa-engine` automatically; typer pinned at 0.15.1 + click 8.1.8 (click 8.2+ broke the `Parameter.make_metavar` API). `make ci` green: ruff format-check + ruff lint + mypy strict (133 source files) + adr-check (7 ADRs incl. new ADR-0007) + pytest (306 default markers; `make test-full` 312 with slow tier). `make coverage` 95.47% (floor 95%); CLI-only coverage 92.32% (over the 90% Phase-02 floor). `make schemas` regenerates 17 schemas; `test_run.schema.json` enum gains `dry_run`. `git status` clean. 0 AI co-author trailers. |
| 03 | done | PASS | ohswedd | 2026-05-27 | All eight tasks (03.01–03.08) shipped on branch `feature/phase-03-report-schemas-reporter`. Wire schemas under `packages/shared-schema/`: `run.schema.json`, `findings.schema.json`, `score.schema.json` (all draft 2020-12); vendored `external/sarif-2.1.0.json` (3 389 lines, official OASIS) and `external/junit.xsd` (permissive Surefire subset). Writers: `engine/reporter/run_writer.py` (write_run + canonical_config_digest + derive_release_decision + summarize_modules_and_findings), `findings_writer.py` (write_findings with PRD §20 evidence enforcement at medium+), `findings_linter.py` (L-FND-001..004 codes), `score_writer.py` (deterministic float formatting, DEFAULT_POLICY overlay), `junit_writer.py` (Surefire mapping + system-out redaction proven by Authorization-header test), `sarif_writer.py` (severity→level mapping, SarifRuleRegistry with synthesized GEN-* fallback), `markdown_writer.py` (deterministic + md_escape over the full `\`*_{}[]()#+-.!|<>` set). Dispatcher: `engine/reporter/dispatcher.py` exposes `Reporter`, `ReportInputs`, `ReporterPlugin` Protocol, `register_reporter_hook`. Lifecycle integration: `RunLifecycle.__init__` registers the hook idempotently on `GENERATE_REPORTS`; status + finished_at finalized at the start of generate_reports; `persist_artifacts` reduced to latest-pointer write; new ctx fields `typed_findings`/`typed_module_results`/`typed_score`/`typed_policy`. `run.json` always written regardless of formats (CLAUDE §11). `json` alias expands to run + findings + score; `html` is a Phase-15 placeholder. Each emit produces one `artifact_emitted` line in audit.log. Goldens: 22 byte-locked fixtures under `tests/golden/reports/` (run.passed/unsafe/dry_run; findings.empty/critical/mixed/redacted; score.pass/blocked/warnings/unsafe/dry_run; junit/passing/failed/empty; sarif/empty/critical/mixed; markdown.passing/blocked/unsafe/dry_run). `make update-goldens` prompts for confirmation; `FORCE=1` bypasses for CI. Schema-validity check (`tests/integration/reporter/test_schemas_are_valid.py`) walks every committed `*.schema.json` (21 entries) + the vendored SARIF schema and validates each against its meta-schema. Hypothesis property tests (slow tier) generate randomized findings and prove findings.json validates, JUnit XML parses, SARIF docs validate against the official schema. `.pre-commit-config.yaml` excludes `tests/golden/` from trailing-whitespace + end-of-file fixers so goldens stay byte-stable. PRD §20 expanded to §20.1 (Persisted artifacts table), §20.2 (Finding evidence requirement), §20.3 (Schema drift guard). ADR-0008 (Report schemas & reporter pipeline) accepted; ADR README index updated to include 0005–0008. `make ci` green (434 passed); `make test-full` 443 passed (+9 slow incl. 3 property); `make coverage` 95.00% (floor met). 0 AI co-author trailers. |
| 04 | done | PASS | ohswedd | 2026-05-28 | All eight tasks (04.01–04.08) shipped on branch `feature/phase-04-typescript-playwright-runtime`. `@sentinelqa/ts-runtime` is wired with the strict tooling (composite tsconfig + tsc-build emit + postbuild shebang, `engines.node ≥ 20`, four subpath exports). Sentinel-ts CLI ships `--help`, `--version`, `run`, `list-tests`, `validate-helpers` with the deterministic exit-code grid (0/1/2/7). Custom Playwright reporter (`src/reporter.ts`) translates `onBegin/onTestBegin/onStepBegin/onStepEnd/onTestEnd/onEnd` into JSONL events; runner (`src/runner.ts`) spawns Playwright with that reporter via the workspace bin (or `npx playwright` fallback), captures stderr and forwards it only on non-zero exit. JSONL protocol: `packages/shared-schema/ts-events.schema.json` (Draft 2020-12, 14 event kinds, envelope `type+schema_version+seq+ts`), `engine/orchestrator/ts_bridge.py` (Pydantic models + `parse_event` + `stream_events` async iterator). PROTOCOL_VERSION=`1.0.0` checked by parity test against `src/protocol.ts`. Helpers (`src/helpers.ts`): `sentinelStep`, `captureEvidence` (screenshot+DOM+HAR refs), `redactedNetwork`, `redactedConsole` (warning→warn level mapping + redacted message + redacted source URL), `captureDomSnapshot` (writes HTML + emits dom.snapshot + returns sha256 AX-tree hash for the Phase-20 Healer), `harConfig` (per-test deterministic HAR path). `sentinelTest` in `src/playwright.ts` extends Playwright's `test` with `sentinel` fixture + opt-in `_network` auto-fixture; `SENTINEL_PLAYWRIGHT_DEFAULTS` matches CLAUDE §21 (trace on-first-retry, screenshot only-on-failure, video retain-on-failure). Semantic-first locator utilities (`src/locators.ts`): `bestLocator` (strategy chain `getByRole→…→getByTitle`), `describeLocator` (role/name/text/landmarks/tagName via `evaluate`), `auditLocatorBrittleness` (ts-morph AST walk; flags `:nth-of-type`, raw XPath, nested-div soup, class-prefix matchers, raw-only-no-semantic catch-all). Redaction parity: `scripts/export-redaction-rules.py` + `scripts/export-redaction-parity.py` + `scripts/export-ts-events-parity.py` write canonical fixtures; `--check` modes are CI drift gates; `.gitleaks.toml` + `detect-private-key` extended for the new fixture paths only (production code still rejected). Fixture sample-app (`packages/ts-runtime/fixtures/`) with `serve.mjs` (Node http) + `index.html` / `success.html` + a gated Chromium smoke (`SENTINELQA_HAS_CHROMIUM=1` runs `fixtures/specs/login.spec.ts`). `make ci` green: 477 Python tests + 105 TS tests = 582. `make test-full` 486 Python (+9 slow inc. 3 hypothesis). `make coverage` Python 95.71%; TS coverage gate enforced in `vitest.config.ts` (88.97 lines / 75.62 branches / floors 85 / 75). ADR-0009 (Python ↔ TS JSONL protocol) accepted; PRD §15 expanded to §15.1–§15.7 documenting sentinel-ts contract, JSONL protocol, evidence defaults, locator strategy, and safety boundary. 0 AI co-author trailers. |
| 05 | done | PASS | ohswedd | 2026-05-28 | All eight tasks (05.01–05.08) shipped on branch `feature/phase-05-discovery-module`. `engine.discovery` ships the HTTP-first MVP: `Crawler` (httpx + robots.txt + token-bucket rate limit + transparent `SentinelQA/<ver>` UA + `X-SentinelQA-Test-Run` header, pluggable `CrawlBackend` Protocol), `DomMapBuilder` (BS4 element extraction, missing-label observations, repeated-component heuristic, unreachable-link detection), `FormsInventory` (fields, validation hints, reCAPTCHA flag, no-secrets), `ApiDetector` (path templating with `[id]`/`[uuid]`/`[hex]`, 5xx detection, JS-bundle referenced-only detection), `AuthBoundaryDetector` (anon + authenticated passes, UI-only-auth + escalation hints, env-var-name-only artifacts), `OpenAPIIngester` (3.x JSON/YAML/URL ingest + meta-validation + cross-check), `GraphQLIngester` (SDL + introspection), ten-rule deterministic `risk_model` + `build_risk_map`. `sentinel discover --url ...` replaces the Phase 02 stub, runs lifecycle steps 1–8, enforces safety BEFORE any I/O, and writes `discovery.json` + `forms.json` + `api.json` + `auth.json` + `risk.json` + `discovery.report.md`. ADR-0010 (Discovery MVP is HTTP-first; SPA Playwright backend re-homed to Phase 17 task 07) accepted. PRD §9.1 expanded with §9.1.1 (MVP delivery). `make ci` green (531 Python + 115 TS = 646 tests). `make coverage` 95.53 % (floor 95 %). 0 AI co-author trailers. |
| 06 | done | PASS | ohswedd | 2026-05-28 | All six tasks (06.01–06.06) shipped on branch `feature/phase-06-planner-module`. `engine.planner` ships the deterministic core (`DeterministicPlanner.plan` over routes/forms/APIs/auth boundaries with the audited rule set), eleven named extractors (`LoginFlowExtractor`, `SignupFlowExtractor`, `LogoutFlowExtractor`, `PasswordResetFlowExtractor`, `CrudFlowExtractor`, `SearchFilterSortFlowExtractor`, `AdminFlowExtractor`, `RoleFlowExtractor`, `FileUploadDownloadFlowExtractor`, `PaymentSandboxFlowExtractor`, `NotificationFlowExtractor`) with deterministic ordering and per-flow confidence (`PROPOSAL_THRESHOLD=0.5` flags `confidence_low`), the `TestPlan`/`CoverageEstimate` domain models with the matching `plan.schema.json` wire envelope, deterministic `plan.json` + `plan.md` writer with sorted-set serialization for byte-stable goldens, the optional `LlmPlanner` Protocol with `NullLlmPlanner` default, two HTTP-only provider adapters (`openai_planner.py`, `anthropic_planner.py`) speaking the OpenAI Chat Completions and Anthropic Messages APIs through `httpx` (no vendor SDK), a versioned locked prompt at `engine/planner/llm_prompts/planner.v1.md` (PROMPT_VERSION=1), strict `_ProposalEnvelope` re-validation, per-run USD budget enforcement (`max_usd_per_run`, default $0.50) with `BudgetExceededError` fallback, and the `sentinel plan --url --from-discovery --llm/--no-llm` CLI command which replaces the Phase 02 stub. `Flow` and `TestCase` domain models gained `description`/`confidence`/`tags`/`extractor`/`source` and `module` respectively; `Flow.tags` is canonicalized to a sorted tuple so plan.json is byte-stable across runs. `make ci` green: 631 Python tests + 115 TS = 746 total. `make coverage` 95.14 % (floor 95 %). Planner-package coverage 97.46 % (floor 90 %). TS coverage gate unchanged: 93.99 % lines / 79.55 % branches (floors 85 / 75). ADR-0011 (planner deterministic vs LLM) accepted; PRD §9.2 expanded with §9.2.1 (MVP delivery). `sentinel.config.yaml.example` documents the new `planner.llm` block. 0 AI co-author trailers. |
| 07 | done | PASS | ohswedd | 2026-05-28 | All seven tasks (07.01–07.07) shipped on branch `feature/phase-07-generator-module`. `engine.generator` ships the deterministic Playwright spec generator: `engine.generator.render` (Jinja2 with `StrictUndefined`, mandatory banner enforcement, `regex_literal`/`regex_pattern`/`js_string` filters), 14 templates under `engine/generator/templates/` (`smoke`, `login`, `signup`, `logout`, `crud_{create,read,update,delete}`, `role_boundary`, `payment_sandbox`, `file_upload`, `api_contract`, `a11y_axe`, `perf_budget`), `engine.generator.page_objects` (one `<RouteName>Page.ts` per route in ≥ 2 flows or with ≥ 3 elements; semantic locators only; `goto()` + `verify()`), `engine.generator.fixtures` (auth + data + global-setup/teardown; env-var-named creds only; data fixture gated on `security.mode=authorized_destructive`), `engine.generator.locator_strategy.audit_specs` (Python wrapper around the new `sentinel-ts audit-locators --file <path>` TS subcommand), `engine.generator.plan_md` (deterministic summary + diff-vs-prior), `engine.generator.pipeline.GeneratorPipeline` (orchestrator with per-extractor dispatch + ID-strip via `_stable_tags`/`_stable_disambiguator` so re-runs are byte-stable), and `engine.generator.writer` (atomic file writer with banner-aware hand-edit guard — unmanaged files raise `OverwriteError` (exit 6) unless `--force`). CLI: `apps/cli/src/sentinel_cli/commands/generate_cmd.py` replaces the Phase-02 stub behind `sentinel generate --url ...` (also `--from-plan` + `--from-discovery`). Lifecycle steps 1–9 enforced; safety policy refuses unsafe targets before any write (exit 4). Audit runs BEFORE writes; failures abort (exit 6). TS additions: `auditLocators(opts)` in `runner.ts` + `audit-locators` subcommand in `cli.ts` with sorted JSON output. Planner cleanup uncovered by adding `engine/planner` to mypy: `tuple` (not `frozenset`) for tags, explicit `Priority` annotation on `form_priority`. Tests (108 new): 48 unit, 21 integration (incl. tsc-acceptance slow tier + audit subprocess parity), 11 CLI integration (incl. JSON / quiet / hand-edit refusal / safety blocking / audit failure paths), 15 byte-locked template goldens, 9 TS subcommand tests. `make ci` green: ruff format-check + lint + mypy strict (257 source files) + adr-check + pytest 735 default + Prettier + ESLint + tsc + Vitest 124 = 859 total. `make coverage` 95.21 % (floor 95 %). Generator-package coverage 96 %+ (floor 90 %). TS coverage 93.80 % lines / 79.46 % branches (floors 85 / 75). ADR-0012 (generated test conventions) accepted; PRD §9.3 expanded with §9.3.1 (MVP delivery). 0 AI co-author trailers. |
| 08 | done | PASS | ohswedd | 2026-05-28 | All seven tasks (08.01–08.07) shipped on branch `feature/phase-08-runner-module`. `engine.runner` ships the dual-runner core: `LocalRunner` (spawns `sentinel-ts run --input <run-config.json>` via `asyncio.create_subprocess_exec`, streams stdout JSONL through `engine.orchestrator.ts_bridge`, captures redacted stderr to `logs/runner.<module>.log`, propagates `SIGINT → SIGTERM → SIGKILL` with configurable grace, idempotent on partial streams), `DockerRunner` (same `RunnerInvocation → RunnerOutcome` contract; pinned to `mcr.microsoft.com/playwright:v1.49.0-jammy`; source RO + run-dir RW mounts; `host.docker.internal:host-gateway` aliased; no `--privileged`, no Docker socket; re-enforces `SafetyPolicy.enforce` BEFORE container launch), the JSONL aggregator (`engine.runner.results.aggregate` translates `run.start`/`test.start`/`test.end`/`evidence`/`run.end` into `RunnerOutcome` with per-test executions, P50/P95 module metrics, environment context, flake-rate, partial-stream tolerance), strict quarantine (`engine.runner.quarantine.Quarantine`: YAML list of `{test_id, reason, expires_at, issue_url}` with `expires_at ≤ today + 14d` cap, `issue_url` http(s)://-only, expired entries rejected at load time per CLAUDE §23), deterministic sharding (`engine.runner.sharding.split_shard` via `sha1(posix_path) % total`; `merge_outcomes` dedups by `test_id`, picks worst module status `errored > failed > incomplete > skipped > passed`), and the new wire envelope `module-results/<module>.json` (`MODULE_RESULTS_SCHEMA_VERSION = "1"`). CLI: `apps/cli/src/sentinel_cli/commands/test_cmd.py` replaces the Phase-02 stub behind `sentinel test` with full option set (`--path`, `--grep`, `--workers`, `--shard`, `--browser`, `--docker`, `--retries`, `--with-generate`, `--url`, `--module`, plus global `--config / --ci / --json / --quiet / --verbose`). `--with-generate` invokes the Phase 05/06/07 chain (discovery → planner → generator) inline before launching the runner. Exit codes wired to the canonical grid: 0 / 1 (flake-rate gate) / 2 (config / no specs) / 4 (`UnsafeTargetError`) / 5 (`sentinel-ts` missing) / 6 (runner failure). Config additions: `runner.workers` (`auto`|int 1–64), `runner.shards` (`N/M` regex-checked), `runner.browser`, `runner.headless`, `runner.timeout_ms`, `runner.docker`, `runner.docker_image`, `runner.retries.{max, backoff_ms}`, `runner.quarantine.{path, max_age_days}`. `Makefile`: new `build-runner-image` target wrapping `docker build` against the pinned Dockerfile (`apps/cli/sentinel/runner/docker/Dockerfile.runner`). Tests (90 new): 50 unit (aggregate/quarantine/retry/sharding/local helpers/docker helpers/results edges/sharding edges), 14 integration runner (local_runner, docker_runner, partial_stream), 13 CLI integration (test_test_command, test_test_with_generate_e2e exercising the full discovery → planner → generator chain via `pytest-httpserver` with a stubbed `LocalRunner`). `make ci` green: ruff format-check + lint + mypy strict (272 source files) + adr-check (13 ADRs incl. new ADR-0013) + pytest 823 default + Prettier + ESLint + tsc + Vitest 124 = 947 total. `make coverage` 95.16 % (floor 95 %). Runner-package coverage ≥ 95 % per file (floor 90 %). TS coverage unchanged at 93.80 % lines / 79.46 % branches (floors 85 / 75). ADR-0013 (Runner architecture: local + Docker, retry, quarantine, sharding) accepted; PRD §9.4 expanded with §9.4.1 (MVP delivery). 0 AI co-author trailers. |
| 09 | done | PASS | ohswedd | 2026-05-28 | All six tasks (09.01–09.06) shipped on branch `feature/phase-09-analyzer-module`. `engine.analyzer` ships the deterministic core: `engine.analyzer.categorize.categorize` runs 11 named rules over the closed PRD §9.5 ten-category set (`app_bug` / `test_bug` / `environment_failure` / `flake` / `data_setup_failure` / `auth_failure` / `api_failure` / `performance_regression` / `security_finding` / `accessibility_violation`) plus an honest `unknown` bucket; every match is preserved as a `secondary`. `categorize_module_error` rehomes the Phase-02 `RunLifecycle.run_modules` catch-all (CLAUDE §10) so module-level errors land on `ModuleOutcome.error_category` / `_confidence` / `_rationale` / `_type` instead of mystery `errored` strings. `engine.analyzer.root_cause.hypothesize` interpolates per-category templates with redaction-safe snippets (URLs stripped of query/fragment + clipped at 80 chars; error messages clipped at 200 chars), confidence is the classifier's (never re-inflated), hypothesis text hard-clipped at 1024 chars, and each category carries a fixed ordered `next_actions` checklist. `engine.analyzer.repro.reproduction` returns credential-free repro steps (auth pulled from `*_env` references, never literal credentials per CLAUDE §33); `build_repro_spec` emits a minimal Playwright TS spec gated by the locked `// SENTINELQA AUTO-GENERATED REPRO SPEC` banner with `process.env["NAME"]` auth and a TODO assertion. `engine.analyzer.retry_decision.should_retry` returns a typed `RetryDecision` (`retry` / `quarantine_candidate` / `no_action`) with a hard two-retry cap regardless of category (CLAUDE §23); test-bug stability escalates to `quarantine_candidate` once retried or when `FailureHistory` shows ≥ 3 of the last recent runs failed against a healthy app. `engine.analyzer.pipeline.Analyzer.analyze` orchestrates all four stages, sorts results by `test_id`, and optionally appends a one-sentence `llm_refinement` from a configured explainer (LLM failures are silently swallowed; deterministic output is preserved). `engine.analyzer.llm_explainer` mirrors the planner adapter exactly: `LlmExplainer` Protocol, `NullLlmExplainer` default, `HttpLlmExplainerBase` for shared budget / cost / API-key wiring, and HTTP-only `OpenAiLlmExplainer` + `AnthropicLlmExplainer` adapters (no vendor SDKs; POSTs via `httpx`). Locked prompt at `engine/analyzer/llm_prompts/explainer.v1.md` (PROMPT_VERSION="1"); strict `{ "refinement": "<= 400 chars" }` envelope validation; spend bounded by `analyzer.llm.max_usd_per_run` (default $0.25); feature OFF by default (`analyzer.llm.enabled: false`) — no LLM constructed, no HTTP client opened, fully air-gapped. Config additions: new `analyzer.llm` block (`AnalyzerConfig` + `AnalyzerLlmConfig`); `sentinel.config.yaml.example` updated. Tests (124 new): 84 unit (categorize, root_cause, repro, retry_decision, signals, pipeline, llm_null + edge cases), 17 integration (`test_llm_explainer_mocked` with `httpx.MockTransport`, `test_repro_replay` for spec contract, `test_pipeline_e2e` for full Runner → Analyzer chain), 4 lifecycle rehome (`test_lifecycle_module_error`). `make ci` green: ruff format-check + lint + mypy strict (299 source files) + adr-check (14 ADRs incl. new ADR-0014) + pytest 963 default + Prettier + ESLint + tsc + Vitest 124 = 1087 total. `make coverage` 95.36 % (floor 95 %). Analyzer-package coverage ≥ 94 % per file (floor 90 %): `categorize` 98 %, `root_cause` 95 %, `repro` 96 %, `retry_decision` 97 %, `pipeline` 98 %, `llm_explainer` 96 %, `signals` 98 %, `models` 100 %, providers 94–97 %. TS coverage unchanged at 93.80 % lines / 79.46 % branches (floors 85 / 75). ADR-0014 (Analyzer rules: categorization, root cause, repro, retry, optional LLM) accepted; PRD §9.5 expanded with §9.5.1 (MVP delivery). 0 AI co-author trailers. |
| 10 | done | PASS | ohswedd | 2026-05-28 | All five tasks (10.01–10.05) shipped on branch `feature/phase-10-functional-module`. `engine.modules.base.SentinelModule` is the new abstract class for every audit module (ADR-0015); the orchestrator's `run_modules` detects instances returned from a factory and drives the seven-step CLAUDE §9 lifecycle, threading per-module options through `ModuleContext` and merging typed findings into `LifecycleContext.typed_findings` / `typed_module_results`. `modules.functional.FunctionalModule` is the first concrete module: self-registers with the process-wide registry on import, plans by walking `tests/sentinel/` for `*.spec.ts`, executes via `LocalRunner` / `DockerRunner` (with a pluggable `runner_factory` for tests), translates failed `TestExecution` records into typed `Finding`s with PRD §20 evidence (falling back to `logs/runner.<module>.log` when the runner didn't capture artifacts), and exposes a `FunctionalModuleOptions` typed dataclass for the CLI. `RunnerInvocation` + `RunConfig` gain a new optional `grep` field (Python + TS sides in parity) that the TS runner forwards to `playwright test --grep <value>`. The generator's canonical tag set (Phase 10.03) now emits, in order, `@p0..p3`, `@module:<name>` (extractor → module lookup), `@flow:<extractor>`, `@risk:<level>`, plus any planner-attached, ID-stripped tags. `modules.functional.tags.TagSelection.resolve` is the single helper that merges slice modes (`smoke / standard / full`) with optional `--grep <user>` — `sentinel functional --mode smoke` → `--grep @p0`, `--mode standard` → `--grep @p0|@p1`, `--mode full` → no filter; combined modes intersect via `(<mode>).*<user>`. CLI: `apps/cli/src/sentinel_cli/commands/functional_cmd.py` replaces the Phase-02 stub behind `sentinel functional` with options `--url / --mode / --grep / --workers / --shard / --retries / --spec-root` and the standard exit-code grid (0 passed, 1 quality gate failed, 2 config/shard/mode error, 4 unsafe target, 5 runner binary missing, 6 runner error). `RunLifecycle.execute` accepts a new `module_options` kwarg (`dict[module, dict]`) and exposes `last_context` so the CLI / future SDK can read typed module results without a disk round-trip. `packages/ts-runtime/fixtures/sample-app-broken/` ships the failing-path fixture for the runner sweep. Tests (61 new): 17 unit module skeleton, 19 unit tag selection, 13 unit helper coverage, 4 integration flow coverage, 5 integration runner sweep, 3 integration lifecycle (SentinelModule wiring + module_options + prerequisite-error path), 12 CLI integration. `make ci` green: ruff format-check + lint + mypy strict (314 source files) + adr-check (15 ADRs incl. new ADR-0015) + pytest 1037 default + Prettier + ESLint + tsc + Vitest 124 = 1161 total. `make coverage` 95.39 % (floor 95 %). functional-package coverage 100 % per file (floor 90 %): `modules/functional/__init__.py` 100 %, `modules/functional/module.py` 100 %, `modules/functional/tags.py` 100 %. TS coverage 93.66 % lines / 78.94 % branches (floors 85 / 75). ADR-0015 (Module contract and functional module) accepted; PRD §10.1 expanded with §10.1.1 (MVP delivery). 0 AI co-author trailers. |
| 11 | done | PASS | ohswedd | 2026-05-28 | All seven tasks (11.01–11.07) shipped on branch `feature/phase-11-accessibility-module`. `modules.accessibility.AccessibilityModule(SentinelModule)` is the second concrete module and the first that does NOT drive Phase 08's Playwright runner — accessibility checks are per-route, so the module pairs the seven-step lifecycle (CLAUDE §9) with a dedicated `A11yRunner` Protocol (`LocalA11yRunner` spawns `sentinel-ts audit-a11y`; `StubA11yRunner` for tests). Route resolution priority: CLI `--routes` → `discovery.json` → `config.accessibility.routes` → CLI-only `("/",)` fallback (the module short-circuits to `skipped` when no caller supplies routes so `sentinel audit` doesn't fire blindly). Typed wire models (`A11yPageResult`, `AxeViolation`, `KeyboardIssue`, `LandmarkIssue`, `AccessibleNameIssue`) with `schema_version="1"` locked under ADR-0016 §3. TS-side helpers (`packages/ts-runtime/src/a11y/{axe,keyboard,landmarks,sr_names,audit}.ts`) mirror the Python check policy; axe-core is intentionally NOT a workspace dep (typed `AxeCoreNotInstalledError` on missing install, documented in PRD §10.4.1). New `sentinel-ts audit-a11y --input <path>` subcommand writes one `<route-slug>.json` per route plus `index.json`; injectable launcher keeps vitest tests Chromium-free. `modules.accessibility.findings.findings_from_pages` translates each `A11yPageResult` into PRD §18.2 Findings with curated remediation (`color-contrast` → 4.5:1 contrast advice, `image-alt` → alt + decorative guidance, etc.), severity mapping `critical|serious→high`, `moderate→medium`, `minor→low`, confidence `0.95` (stable) / `0.6` (experimental) / `0.9` (deterministic checks). Findings carry per-route artifact paths as evidence with fallback to `logs/runner.accessibility.log`. CLI: `sentinel a11y` runs the canonical RunLifecycle with options `--url / --routes / --axe-tags / --discovery`; exit codes 0 (no high/critical findings), 1 (quality gate failed), 2 (config/CLI usage), 4 (unsafe target), 5 (sentinel-ts missing), 6 (runner failure). CLAUDE §28 enforced via `tests/security/test_no_wcag_compliance_claims.py` which greps `modules/accessibility/` + `packages/ts-runtime/src/a11y/` on every CI run; every product output begins with "Automated accessibility check found". Tests (95 new Python + 12 new TS): 18 unit module skeleton (lifecycle, options resolution, registry idempotency, synthetic-outcome path), 12 unit findings (severity, confidence, evidence fallback, curated remediation, wording guard), 5 unit models (round-trip serialisation), 12 unit axe normaliser (impact defaults, tag filtering, target coercion, experimental flag), 8 unit runner (sentinel-ts resolution, subprocess failure, protocol-violation guard), 17 integration (axe + keyboard + landmark + sr-name with canned fixture payloads), 13 CLI integration (every exit-code branch). `make ci` green: ruff format-check + lint + mypy strict (337 source files) + adr-check (16 ADRs incl. new ADR-0016) + pytest 1124 default + Prettier + ESLint + tsc + Vitest 173 = 1297 total. `make coverage` 95.35 % (floor 95 %). accessibility-package coverage 96.04 % (floor 85 %): every file ≥ 93 %, lowest is `module.py` at 93 %. TS coverage 90.2 % lines / 79.96 % branches (floors 85 / 75); per-file `sr_names.ts` is 41 % lines because the body of `readInteractiveSamples` is a `page.evaluate(...)` IIFE that only executes inside a real browser (its policy mirror in Python is fully covered). ADR-0016 (Accessibility module — axe-core + deterministic checks) accepted; PRD §10.4 expanded with §10.4.1 (MVP delivery). 0 AI co-author trailers. |
| 12 | done | PASS | ohswedd | 2026-05-28 | All seven tasks (12.01–12.07) shipped on branch `feature/phase-12-performance-module`. `modules.performance.PerformanceModule(SentinelModule)` is the third concrete module and the second to use the runner-Protocol pattern from ADR-0016: `execute()` calls an injected `PerformanceRunner` (production: `LocalPerformanceRunner`; tests: `StubPerformanceRunner`). Each PRD §10.5 capability owns a pure-Python deterministic evaluator: `page_budget.evaluate_page_budgets` (median LCP/TTFB/INP/CLS), `api_latency.evaluate_api_latency` (P50/P95 per templated endpoint, `min_samples=5`), `bundle_cpu.evaluate_bundle_size` + `bundle_cpu.evaluate_long_tasks`, `nav_stability.evaluate_nav_stability` (first-to-last DOM + heap growth pct). Severity policy: page/CPU/bundle overage >50% → high else medium; API P95 overage >100% → high else medium; nav-stability is `low` with confidence `0.5` (heuristic per CLAUDE §27). New typed wire models (`PERF_RESULT_SCHEMA_VERSION="1"`) — `PageMetricSample`, `ApiEndpointSummary`, `BundleSummary`, `LongTaskSummary`, `NavStabilitySample`, `PerformancePageResult`, `PerformanceRunOutcome`. New `sentinel-ts audit-perf --input <path>` subcommand: deterministic JSON config, injectable Chromium launcher (vitest-friendly), per-route PerformanceObservers (LCP/CLS/INP/longtask), TTFB from `PerformanceNavigationTiming`, network instrumentation via the Playwright `response` event (JS bundle vs API classification), repeated-nav loop, writes `perf/<route-slug>.json` + `perf/index.json`. `sentinel perf` CLI replaces the Phase-02 stub: options `--url / --routes / --samples / --repeated-nav-samples / --discovery`; exit codes 0/1/2/4/5/6. CLAUDE §27 enforced everywhere — every finding description begins with "Synthetic performance check" and includes an explicit "not Real-User Monitoring" disclaimer. `tests/security/test_synthetic_perf_labeling.py` forbids stronger RUM claims in product output. Config: new `performance` block with `routes`, `samples`, `repeated_nav_samples`, `api_min_samples_for_p95`, `request_timeout_seconds`, `api_path_allowlist`, plus four new budget fields (`ttfb_ms`, `long_task_total_ms`, `dom_growth_pct`, `memory_growth_pct`); `sentinel.config.yaml.example` documents the full surface. `make ci` green: ruff format-check + lint + mypy strict (344 source files) + adr-check (17 ADRs incl. new ADR-0017) + pytest 1257 default + Prettier + ESLint + tsc + Vitest 228 = 1485 total. `make coverage` 95.32 % (floor 95 %). performance-package coverage 97.17 % (floor 85 %); every file ≥ 93 %. TS coverage 91.46 % lines / 80.27 % branches (floors 85 / 75); `src/perf` 95.4 % lines / 81.09 % branches / 95.4 % statements. ADR-0017 (Performance module — synthetic page/API/CPU/leak budgets) accepted; PRD §10.5 expanded with §10.5.1 (MVP delivery); ADR README index updated. 0 AI co-author trailers. |
| 13 | done | PASS | ohswedd | 2026-05-28 | All thirteen tasks (13.01–13.13) shipped on branch `feature/phase-13-security-safe-module`. `modules.security.SecurityModule(SentinelModule)` is the fourth concrete module and runs the seven-step CLAUDE §9 lifecycle through a shared `CheckContext` (httpx.Client + target + SafetyDecision + audit-log path + route set). Eleven per-check files under `modules/security/checks/` (`headers`, `cookies`, `cors`, `csrf`, `xss_reflected`, `xss_stored` gated, `sqli` gated, `idor` gated, `frontend_secrets`, `deps`, `sast`). Stored XSS requires `security.mode=authorized_destructive` + valid proof-of-authorization; SQLi requires local target OR destructive + proof; SAST is opt-in via both `security.checks.sast` and `security.dependency_scanners.semgrep`. Rule catalog `modules/security/rules.py` exposes 23 stable `SEC-*` IDs registered with the Phase-03 SARIF writer on import. Wire format `SECURITY_RESULT_SCHEMA_VERSION="1"` persisted under `<run-dir>/security/<check>.json` + `index.json`. Safety contract: every `run_*` function begins with `SafetyPolicy().enforce(...)` or an explicit precondition gate; the AST guard in `tests/security/test_module_calls_policy.py` enforces this on every CI run, and `tests/security/test_security_forbidden_flags.py` keeps stealth/evasion options off the new CLI surface. CLI `sentinel security` replaces the Phase-02 stub with options `--url / --routes / --discovery / --mode / --proof-of-authorization / --checks` and the canonical exit-code grid (0/1/2/4/5/6). Config additions: new `security.checks` block, `security.dependency_scanners` block, `security.routes`, `security.request_timeout_seconds`, and `auth.second_user` block; `sentinel.config.yaml.example` documents the full surface. `make ci` green: ruff format-check + lint + mypy strict + adr-check (18 ADRs incl. new ADR-0018) + pytest 1454 default + Prettier + ESLint + tsc + Vitest 228 = 1682 total. `make coverage` 95.05 % (floor 95 %). `modules.security` package coverage 91.17 % (floor 90 %). TS coverage unchanged at 91.46 % lines / 80.27 % branches (floors 85 / 75). ADR-0018 (Security module — safe HTTP checks, gated probes, dep + SAST adapters) accepted; PRD §10.7 expanded with §10.7.1 (MVP delivery); ADR README index updated. 0 AI co-author trailers. |
| 14 | done | PASS | ohswedd | 2026-05-29 | All seven tasks (14.01–14.07) shipped on branch `feature/phase-14-quality-scoring`. `engine.scoring` is the new package owning reproducible score + release decision computation: `model.compute_score` (eight PRD §19.1 axes with weighted average; per-axis component = `max(0, 100 - Σ severity_penalty(finding))`; `flake_risk` derived from `ModuleResult.metrics["flake_rate"]` via `100 * (1 - min(1, avg/policy.max_flake_rate))`; default weights match PRD §19.1; severity penalties default to PRD §19.2 midpoints with new `policy.severity_penalty_high/medium/low` knobs in the 10..25 / 3..10 / 1..3 ranges; critical is fixed at 30), `blockers.compute_blockers` (CLAUDE §25 rules `critical_finding`, `security_high`, `p0_flow_failed`, `too_many_p1_failures`; P0/P1 detection parses `@p0..p3` out of the finding title via `model.finding_priority`), `decision.decide` (typed `PolicyDecision` with priority `unsafe_blocked → unsafe_target_rejected`, `incomplete/dry_run → inconclusive`, blockers → `blocked`, `score<min → blocked`, medium present → `pass_with_warnings`, otherwise `pass`; rule narratives in `PolicyDecision.reasons`), `policy_gate.apply_policy_gate` (one-call orchestrator helper) + `register_scoring_hooks` (idempotent registration on `CALCULATE_QUALITY_SCORE` + `APPLY_QUALITY_GATES`; the score hook derives an effective status from `module_outcomes` because `LifecycleContext.status` is not finalized until `generate_reports`; the gate hook flips `quality_gate_passed=False` only on `blocked`). `engine.orchestrator.registry.ModuleRegistry.clear()` now also clears the per-hook sentinel flags so tests that build fresh lifecycles re-register defaults. `sentinel report --explain-score` replaces the Phase-15 stub for the explain path only: human / JSON / Markdown output of per-axis math + severity penalties + blockers + policy thresholds with `score-explanation.md` written next to the source `score.json`; calling `sentinel report` without `--explain-score` still exits 7 (no fake completion). Tests: 70+ new (23 score-model + 13 blockers + 10 decision + 7 policy_gate + 5 hook guards + 4 integration lifecycle + 5 replay parametrize + 9 CLI integration); 1 hypothesis property test at 5000 examples (slow tier) proving byte-identical `score.json` across same inputs; three canonical byte-locked replay goldens under `tests/integration/scoring/expected/` (excluded from prettier + the trailing-whitespace hook so byte equality holds). `make ci` green: ruff format-check + lint + mypy strict + adr-check (19 ADRs incl. new ADR-0019) + pytest 1509 default + Prettier + ESLint + tsc + Vitest 228 = 1737 total. `make coverage` 95.17 % (floor 95 %). `engine.scoring` package coverage 96.62 % (floor 95 %): every file ≥ 88 %. TS coverage unchanged at 91.46 % lines / 80.27 % branches (floors 85 / 75). ADR-0019 (Quality scoring — reproducible 0..100 score + policy gate) accepted; PRD §19 expanded with §19.5 (MVP delivery); ADR README index updated. 0 AI co-author trailers. |
| 15 | done | PASS | ohswedd | 2026-05-29 | All seven tasks (15.01–15.07) shipped on branch `feature/phase-15-html-json-reports`. HTML report: self-contained Jinja2 template + inline CSS/JS (each ≤ 30 KB), light + dark via `prefers-color-scheme`, header (score + decision badges + run id + target + duration), summary, critical-blocker section pinned at top, full findings table with severity/module/search filters (vanilla JS, no framework), per-module result cards, evidence drawer (lazy-loads relative paths), audit-trail view (redacted JSONL → typed `AuditEntry`), optional trends overlay (inline SVG sparklines, no JS chart lib), footer with config digest + schema versions + companion-artifact links. `HTML_REPORT_SCHEMA_VERSION="1"` locked. Offline guarantee enforced by `tests/integration/reporter/test_html_self_contained.py` (regex sweep for external `<link>`/`<script>`/`<img>` URLs). Structural a11y self-test asserts main landmark, skip link, hierarchical headings, alt text, accessible names on form controls, aria-labelledby target existence. PR comment: `engine.reporter.pr_comment.render_pr_comment` returns GitHub-flavored Markdown anchored with `<!-- sentinelqa:pr-comment -->` for upsert; all user strings escape through `md_escape`; capped at GitHub's 65 535-char limit with a truncation notice. Trends: `engine.reporter.trends.compute_trends(runs_root)` walks `.sentinel/runs/<id>/` newest-first, derives total-score series, per-module pass-rate series, and top recurring finding IDs across last 10 runs; sparklines inline SVG; trends hidden when fewer than two prior runs exist. Audit view: `engine.reporter.audit_view.load_audit_entries` reads redacted JSONL, normalizes to `AuditEntry`, drops malformed lines silently. Slack summary: `engine.reporter.slack.render_slack_payload` returns Block Kit JSON (header, six-field summary, optional top-blockers, context, optional actions); validated against vendored `packages/shared-schema/external/slack-block-kit.schema.json`. CLI: `sentinel report --latest / --run-id / --format html,json,sarif,junit,md / --open / --explain-score` replaces the Phase-14 stub for non-explain calls; re-render reads persisted artifacts and rewrites requested formats through the existing dispatcher; **idempotent** (same inputs → byte-identical outputs); does NOT write audit-log entries (audit log is one-shot per CLAUDE §11); `--open` skipped in CI. Exit codes: 0 success, 2 missing run / empty format set, 7 internal parse error. Dispatcher: `SUPPORTED_FORMATS` gains `"html"`; Phase-03 placeholder removed; HTML emission reads audit log + computes trends before rendering so embeds them. `make ci` green: ruff format-check + lint + mypy strict (417 source files) + adr-check (20 ADRs incl. new ADR-0020) + pytest 1618 default + Prettier + ESLint + tsc + Vitest 228 = 1846 total. `make coverage` 95.25 % (floor 95 %). `engine.reporter` package coverage 96 % (floor 85 %). TS coverage unchanged at 91.46 % lines / 80.27 % branches (floors 85 / 75). ADR-0020 (HTML, PR-comment, Slack, and trends reports) accepted; PRD §9.7 expanded with §9.7.1 (MVP delivery); ADR README index updated. `.gitleaks.toml` gains the new audit-view test path on the allowlist (test asserts redacter strips a fake Stripe-style key from the persisted audit log; literal needed for negative assertion). 0 AI co-author trailers. |
| 16 | done | PASS | ohswedd | 2026-05-29 | All seven tasks (16.01–16.07) shipped on branch `feature/phase-16-python-sdk`. SDK package directory migrated from `packages/python-sdk/src/sentinel/` to `…/sentinelqa/` so PRD §14.1's `from sentinelqa import Sentinel` works literally; hatch build + workspace dependency on `sentinelqa-engine` updated; pyproject bumped to 0.1.0. `Sentinel` facade (`_facade.py`): typed sync API plus `async_<name>` counterparts for `discover/plan/generate_tests/audit/run_plan/report/verify_fix`; sync forms are `asyncio.run(self.async_<name>(...))` so the implementation lives once. `verify_fix` raises `NotImplementedError` with a Phase-20 pointer (CLAUDE.md §37 — interfaces awaiting concrete adapters are explicitly permitted). Public models (`_models.py`): frozen Pydantic `AuditResult` (`SCHEMA_VERSION` pinned to `RUN_SCHEMA_VERSION`, `extra="forbid"`) with derived views (`passed` / `failures` / `blockers` / `findings_by_severity` / `findings_by_module`) and `to_agent_messages()`; `QualityGate.from_config(policy)` and `Policy.from_config(root)` re-expose config posture as immutable views. Agent messages (`_agent_messages.py` + `agent.py`): every public exception, every `Finding`, every `RepairSuggestion`, and `AuditResult` produce stable, redacted dicts versioned by `AGENT_MESSAGE_SCHEMA_VERSION`; `Finding.to_agent_message()` and `RepairSuggestion.to_agent_message()` are now methods on the domain entities (delegating to the SDK builder) so the SDK and engine emit byte-equal dicts; `AuditResult.to_agent_messages()` returns a fixed stream (`run_summary` → `finding[]` → `blocker_summary` → `next_actions`); `sentinelqa.agent.format(messages, *, format="ndjson"|"jsonl"|"list")` serialises deterministically (sorted keys, no ASCII escapes). Errors (`errors.py`): re-exports `SentinelError` + 11 subclasses; `from_dict(agent_message)` reconstructs the most specific subclass for a given code (round-trip preserves `code` / `exit_code` / `message` / `suggested_fix` / `context`). Internals (`_internal/runtime.py`): config loader + LifecycleContext → AuditResult adapter; lazy-imported by facade methods so `import sentinelqa` stays cheap. Lazy-import contract enforced by `tests/unit/sdk/test_import_time.py` (subprocess measurement under 600 ms ceiling; local dev ~80 ms; budget per task 16.02 is 200 ms). Public surface gate: `packages/python-sdk/api-snapshot.json` (`make sdk-api-snapshot` regenerates; `tests/unit/sdk/test_api_snapshot.py` diffs the committed bytes against a fresh dump); `packages/python-sdk/__deprecation_policy.md` documents the breaking-change process (ADR + deprecation window + snapshot refresh) required by CLAUDE.md §14 / §40. `.prettierignore` excludes the snapshot. Tests (138 new): 89 unit (public surface, import time, facade construction, audit-result views, error round-trip, agent helpers, API snapshot gate, facade plumbing), 39 integration (audit against stub registry, sync/async parity, PRD §14.1 + §14.2 reproduced verbatim), 10 byte-locked agent-message goldens under `tests/golden/sdk/expected/`. `tests/unit/test_smoke.py` updated: was asserting the placeholder `sentinel` package was empty; now asserts `sentinelqa` exposes `Sentinel` + `AuditResult`. `make ci` green: ruff format-check + lint + mypy strict (439 source files) + adr-check (21 ADRs incl. new ADR-0021) + pytest 1742 default + Prettier + ESLint + tsc + Vitest 228 = 1970 total. `make coverage` 95.34 % (floor 95 %). `packages/python-sdk/src/sentinelqa` package coverage: `_errors.py` 100 %, `_internal/__init__.py` 100 %, `_models.py` 100 %, `agent.py` 100 %, `errors.py` 100 %, `_internal/runtime.py` 93 %, `_facade.py` 96 %, `_agent_messages.py` 97 %, `__init__.py` 85 % (PackageNotFoundError fallback) — average ≥ 95 %, well above the Phase-16 floor of 90 %. TS coverage unchanged at 91.46 % lines / 80.27 % branches (floors 85 / 75). ADR-0021 (Public SDK surface — `sentinelqa` / `sentinelqa.errors` / `sentinelqa.agent` + snapshot gate) accepted; PRD §14 expanded with §14.5 (MVP delivery); ADR README index updated. `docs/user/error-codes.md` gains a "Python SDK round-trip" section. 0 AI co-author trailers. |
| 17 | done | PASS | ohswedd | 2026-05-29 | All seven tasks (17.01–17.07) shipped on branch `feature/phase-17-ci-integration`. `engine/ci/` ships the integration core: `modes.apply_mode(config, mode, fail_under=None)` resolves PRD §21.3 presets (`fast`/`standard`/`full`/`nightly`/`release`) into a typed `ModePlan` (modules + grep + policy overrides); `diff_aware.select_from_files` translates a git diff into impacted routes / endpoints / specs via deterministic Next.js (App + Pages Router) and Vite heuristics, with broad-impact (lockfile / framework config / Dockerfile) and volume (>50 files) tripwires forcing fallback to full mode. Smoke (`@p0`) is always present. `sentinel ci` replaces the Phase-02 stub: `--url / --mode / --diff / --fail-under / --grep / --output`, always forces `--ci=True`, persists `<run-dir>/ci.json` sidecar with mode + diff range + resolved selection + policy overrides; exit codes 0 / 1 / 2 / 4 / 5 / 6. GitHub composite Action (`integrations/github/action.yml`) inputs (`url` required, `config`, `mode`, `fail-under`, `diff`, python/node versions, sentinelqa-version, install-playwright, upload-artifacts, upload-sarif, artifact-name, retention, working-directory) + outputs (`quality-score`, `release-decision`, `report-html-url`) + reusable workflow at `integrations/github/workflows/sentinel-pr.yml`. GitLab template (`integrations/gitlab/.gitlab-ci.sentinel.yml`) `.sentinelqa` job extendable via `extends:`, caches pip + pnpm + node_modules, uploads JUnit (native test reports) + Code Quality, refuses empty URLs (exit 4). PR / MR posters (`integrations/github/post_pr_comment.py`, `integrations/gitlab/post_mr_note.py`) use `urllib` only (no `requests` dep per CLAUDE §35), upsert via the `<!-- sentinelqa:pr-comment -->` anchor (shared with the Phase 15 reporter), read tokens from env vars only, retry with exponential backoff on 429 / 5xx (Retry-After honored), refuse to post bodies missing the anchor. Phase 17 task 07 lights up the second discovery backend (ADR-0010 follow-up #1 resolved): TS-side `sentinel-ts discover --config <path|->` drives Chromium with a pluggable launcher (dynamic-imported `@playwright/test`; tests inject a stub); two new JSONL event kinds (`discovery.page`, `discovery.endpoint`) added to `packages/shared-schema/ts-events.schema.json`, the Python parser registry, and the cross-language parity fixture (events placed before `run.end` so existing first/last assertions hold). Python-side `engine.discovery.backends.playwright_backend.PlaywrightCrawlBackend` implements the `CrawlBackend` Protocol; translation is one-to-one — every `discovery.page` event becomes one `CrawlPage` so downstream detectors (DOM map, forms, API detector) see an identical shape. `discovery.engine: "playwright"` is now live in the config schema (no longer reserved); the discover CLI constructs the backend lazily. Missing `sentinel-ts` surfaces as `DependencyMissingError` (exit code 5). New GitHub Actions lane `discovery-playwright (gated)` runs `tests/integration/discovery/test_playwright_backend_spa.py` against `packages/ts-runtime/fixtures/spa/` under `SENTINELQA_HAS_CHROMIUM=1`. Backend parity test (`tests/integration/discovery/test_backend_parity.py`) drives both backends against the same SSR fixture (httpx-served via `pytest_httpserver`) and asserts equivalent `CrawlResult` shapes. `make ci` green: ruff format-check + lint + mypy strict (456 source files) + adr-check (22 ADRs incl. new ADR-0022) + pytest 1848 default + 1 skipped (CSR SPA gate) + Prettier + ESLint + tsc + Vitest 236 = 2084 total. `make coverage` 95.08 % (floor 95 %). `engine.ci` package coverage 93–100 % per file (floor 85 %). `integrations/github` poster 92 %, `integrations/gitlab` poster 91 % (floor 85 % per task 17.06). TS coverage 87.47 % lines / 78.31 % branches (floors 85 / 75). ADR-0022 (CI integration — modes, diff-aware selection, posters, Action) accepted; ADR-0010 follow-up #1 marked Resolved; ADR README index updated. PRD §9.1 expanded with §9.1.2 (Playwright backend delivery); §15.3 sentinel-ts command table extended with `discover` + earlier subcommands; §15.4 documents the sixteen event kinds; §21.4 records the Phase 17 CI surface. `sentinel.config.yaml.example` documents the live `discovery.engine` choice. CI hermeticity fix: `test_ci_diff_is_persisted_in_metadata` monkeypatches `select_from_git` so shallow CI clones don't fail; gated discovery lane exposes `sentinel-ts` via a wrapper script at `$HOME/.local/bin/` since pnpm doesn't symlink workspace-package bins. 0 AI co-author trailers. |
| 18 | done | PASS | ohswedd | 2026-05-29 | All six tasks (18.01–18.06) shipped on branch `feature/phase-18-mcp-agent-interface`. `packages/mcp-server` ships the pure-Python MCP server speaking JSON-RPC 2.0 over NDJSON-framed stdio at protocol `2024-11-05` — zero new runtime dependencies beyond `sentinelqa`, `sentinelqa-engine`, and the existing Pydantic 2.10.x pin (ADR-0023). All twelve PRD §16.1 tools registered (`discover`, `plan`, `generate_tests`, `run_tests`, `audit`, `security_audit`, `performance_audit`, `accessibility_audit`, `read_report`, `explain_failure`, `suggest_fix`, `verify_fix`) plus a `sentinel.ping` health check. `AgentEnvelope` wire shape (`schema_version`, `tool`, `result`, `errors`, `evidence_refs`) locked at `packages/shared-schema/agent-envelope.schema.json` and pinned by the byte-stable golden at `tests/golden/mcp/expected/ping_success.json`. `sentinel.verify_fix` ships the agent-observable loop: re-run the prior audit against the current working tree, diff findings by stable fingerprint (`module`\|`category`\|`title`\|`location.file`\|`location.selector`), and return one of four decisions (`fix_verified` / `partial` / `regressed` / `still_failing`) — the Healer's apply-fix logic still lands in Phase 20. `sentinel mcp` CLI replaces the Phase-02 stub with options `--stdio` (default), `--http <PORT>` (loopback-only — refuses any non-loopback bind with exit 4), `--config`, `--log-level`. Exit-code grid 0 / 2 / 4 / 7. Logs go to stderr only — stdout is reserved for MCP wire bytes (CLAUDE.md §13). Safety contract enforced via `sentinelqa_mcp.tools._safety.enforce_url` and an AST guard at `tests/security/test_mcp_safety.py` that runs on every CI pass — every URL-bearing tool runs `SafetyPolicy.enforce` before any SDK call; unsafe targets surface as envelope errors (`code=UNSAFE_TARGET` / `exit_code=4`); destructive checks require the loaded config to opt in AND supply a valid `target.proof_of_authorization`. `sentinel.read_report` reads a single top-level artifact under a run directory (path traversal rejected with exit code 2, files ≤ 256 KiB returned utf-8, larger files truncated, binary returns as hex). `make ci` green: ruff format-check + lint + mypy strict (482 source files) + adr-check (23 ADRs incl. new ADR-0023) + pytest 1962 default + 1 skipped + Prettier + ESLint + tsc + Vitest 244 = 2206 total. `make coverage` 95.03 % (floor 95 %). `packages/mcp-server` per-file: `_safety.py` / `audit.py` / `accessibility_audit.py` / `performance_audit.py` / `security_audit.py` / `generate_tests.py` / `ping.py` / `protocol.py` / `errors.py` / `__init__.py` 100 %; `tools/__init__.py` 97 %; `read_report.py` 95 %; `envelope.py` 94 %; `verify_fix.py` 93 %; `server.py` / `__main__.py` 86 %; `transport.py` 86 % (per-file floor 85 %). TS coverage 87.47 % lines / 78.31 % branches (floors 85 / 75). ADR-0023 (MCP & agent interface — stdlib JSON-RPC server, twelve sentinel.\* tools, envelope contract) accepted; PRD §16 expanded with §16.4 (MVP delivery). `docs/user/mcp.md` shipped + `examples/mcp-claude-desktop/` claude-desktop config and walkthrough. CI hermeticity fix: pyproject `filterwarnings` ignores `ResourceWarning` + `PytestUnraisableExceptionWarning` (Linux selector fds linger until GC; symptom, not product bug); `tests/integration/cli/test_mcp.py` strips ANSI codes before assertions (CI renders Typer help with rich formatting). 0 AI co-author trailers. |
| 19 | done | PASS | ohswedd | 2026-05-29 | All fifteen tasks (19.01–19.15) shipped on branch `feature/phase-19-llm-code-audit-module`. `modules.llm_audit.LlmAuditModule(SentinelModule)` is the fifth concrete module and the first that ships without its own runner Protocol — every check is a pure-Python function over already-captured signals (Phase 05 discovery output, optional runner artifacts, optional source root). Sixteen stable `LLM-*` rule IDs live in `modules/llm_audit/rules.py` (`LLM-DEAD-BTN`, `LLM-FAKE-ROUTE`, `LLM-FAKE-ENDPOINT`, `LLM-MOCK-DATA-SHIPPED`, `LLM-FORM-NO-SUBMIT`, `LLM-INCOMPLETE-CRUD`, `LLM-UI-ONLY-AUTH` critical, `LLM-HARDCODED-CRED`, `LLM-CLIENT-SECRET-STORAGE`, `LLM-NO-LOADING-STATE`, `LLM-NO-ERROR-STATE`, `LLM-VALIDATION-MISMATCH-BACKEND-ACCEPTS`, `LLM-VALIDATION-MISMATCH-FRONTEND-MISSING`, `LLM-PLACEHOLDER-TEXT`, `LLM-CONSOLE-ERROR-IGNORED`, `LLM-UNHANDLED-PROMISE`). Hardcoded-credential snippets are double-redacted before persistence — the matched span is replaced with `[REDACTED:hardcoded_credential]` then the line passes through `engine.policy.redaction.redact`, with `tests/integration/modules/llm_audit/test_hardcoded_creds.py` asserting no literal credential survives in findings (CLAUDE.md §33). Module status policy: `skipped` when no check had any signal, `failed` when any high/critical finding fires, otherwise `passed`. CLI `sentinel llm-audit` replaces the Phase-02 stub with options `--url / --discovery / --signals / --checks / --third-party-hosts` and the canonical exit-code grid (0 success or skipped, 1 quality gate failed, 2 invalid CLI / config, 4 unsafe target, 6 module error, 7 internal error). Reporter differentiator: `engine.reporter.html_writer` adds an `llm_audit` context block driving a dedicated "LLM-Code Audit" section in `report.html`, and `engine.reporter.pr_comment._render_llm_audit_section` emits the matching Markdown table; both renderers stay silent on runs without `llm_audit` activity. New broken-fixture catalogue under `tests/fixtures/llm_audit_broken/` (JSON-only, hermetic) + parallel HTML examples under `packages/ts-runtime/fixtures/sample-app-llm-broken/`; the broken-fixture sweep asserts ≥ 11 of 13 PRD §10.9 checks fire end-to-end. `.gitleaks.toml` extended to allow the deliberate fake credentials in the new tests; production code remains forbidden. `make ci` green: ruff format-check + lint + mypy strict (495+ source files) + adr-check (24 ADRs incl. new ADR-0024) + pytest 2114 default + Prettier + ESLint + tsc + Vitest 236 = 2350 total. `make coverage` 95.05 % (floor 95 %). `modules.llm_audit` per-file coverage ≥ 91 % (floor 90 %): `__init__.py` / `checks/__init__.py` / `models.py` / `rules.py` / `options.py` / `findings.py` 100 %; `dead_buttons.py` / `forms_no_submit.py` / `incomplete_crud.py` / `localstorage_secrets.py` / `ui_only_auth.py` / `validation_mismatch.py` 100 %; `loading_error_states.py` 97 %; `mock_data.py` 96 %; `module.py` 95 %; `coming_soon.py` 94 %; `fake_routes.py` 93 %; `inputs.py` 92 %; `console_errors.py` 91 %; `hardcoded_creds.py` 91 %. TS coverage unchanged at 87.47 % lines / 78.31 % branches (floors 85 / 75). ADR-0024 (LLM-Code audit module — heuristics, signal contract, report differentiator) accepted; PRD §10.9 expanded with §10.9.1 (MVP delivery); ADR README index updated. 0 AI co-author trailers. |
| 20 | done | PASS | ohswedd | 2026-05-29 | All nine tasks (20.01–20.09) shipped on branch `feature/phase-20-healer-self-repair`. `engine.healer` ships the Phase-20 self-repair MVP (ADR-0025): three deterministic proposers (`propose_locator_repair` with confidence tiers 0.5 / 0.7 / 0.75 / 0.9 / 0.95; `propose_wait_repair` translating `await page.waitForTimeout(...)` to reliance on the following `await expect(...).toBeVisible/toHaveText/...` with confidence tiers 0.3 / 0.6 / 0.9; `propose_fixture_repair` with `missing_entity` 0.85 and `contract_drift` 0.7), the structural assertion-weakening guard (`assert_no_assertion_weakening` counts `expect(` / `.toBe*` / `.toHave*` / `.toEqual` / `.toMatch` / `.toContain` after stripping `//` and `/* */` comments; raises `AssertionWeakeningError` on any decrease unless `allow_weaken=True`), banner-aware hand-edit detection (`detect_banner_status` parses `// SENTINELQA AUTO-GENERATED` + `// generated_at:` ISO timestamp; mtime drift > 5s → hand-edited; missing banner → hand-edited; refused regardless of mode), auto-apply gating (`decide_auto_apply` over `off / safe / aggressive` × banner × `requires_human_review` × confidence threshold × `allow_weaken` returns a typed `AutoApplyDecision` with a one-sentence audit-log reason), atomic JSON writer (`write_proposal` + `write_index` under `<run-dir>/healer/`), and the `Healer.propose(failure, inputs, ctx)` facade that returns proposals sorted by `(kind, id)` for determinism. `RepairProposal` wire format extends the Phase-01 `RepairSuggestion` with `kind / target_test_line / unified_diff / descriptor` and is locked at `packages/shared-schema/repair-proposal.schema.json` (Draft 2020-12, `x-sentinelqa-schema-version "1"`); `RPR-*` is already a registered prefix in `engine.domain.ids.ENTITY_PREFIXES`. Analyzer ↔ Healer routing: new `engine.analyzer.pipeline.is_healer_candidate(result)` returns True only for `test_bug` with confidence ≥ 0.5 — the Analyzer remains a pure-function pipeline; the calling layer (CLI / SDK) invokes the Healer. CLI: `apps/cli/src/sentinel_cli/commands/fix_cmd.py` replaces the Phase-02 stub behind `sentinel fix` with options `--latest/--no-latest`, `--run RUN-...`, `--apply none|safe|aggressive`, `--dry-run`, `--allow-weaken`, `--review-only`, `--threshold 0.5..1.0`; default is review-only; applied repairs log to `audit.log` with the gating decision reason verbatim (CLAUDE §11); exit codes 0 / 2 (config / CLI usage) / 6 (apply failure); JSON mode emits a single-line summary with `{run_dir, count, applied, reviewed, skipped, errors}`. MCP integration: `sentinel.suggest_fix` now surfaces persisted healer proposals for the finding's target file alongside the module's `recommendation` / `suggested_fix`; `sentinel.verify_fix` (ADR-0023) loop end-to-end verified by `tests/integration/healer/test_verify_fix_loop.py`. Config: new `healer:` block (`auto_apply: off|safe|aggressive`, default `off`; `auto_apply_threshold: 0.5..1.0`, default `0.9`); `sentinel.config.yaml.example` documents the surface. Tests (108 new): 89 unit (skeleton 7 + locator 10 + wait 10 + fixture 4 + writer 14 + gating 11 + banner 5 + diff guard 8 + config 5 + analyzer routing 12 + apply safety 3), 5 golden JSON-Schema validation, 13 CLI integration (every documented exit-code path + JSON mode + audit log), 2 MCP verify_fix loop. `make ci` green: ruff format-check + lint + mypy strict (537 source files) + adr-check (25 ADRs incl. new ADR-0025) + pytest 2222 default + 1 skipped + Prettier + ESLint + tsc + Vitest 236 = 2458 total. `make coverage` 95.04 % (floor 95 %). `engine.healer` per-file: `__init__.py` 100 %, `fixture_repair.py` 100 %, `gating.py` 100 %, `models.py` 100 %, `writer.py` 100 %, `wait_repair.py` 99 %, `locator_repair.py` 97 %, `pipeline.py` 97 %, `banner.py` 94 %, `diff.py` 93 % (floor 90 %). `apps/cli/.../fix_cmd.py` 87 % (covers all exit-code paths). TS coverage unchanged at 87.44 % lines / 78.31 % branches (floors 85 / 75). ADR-0025 (Healer / Self-Repair — deterministic proposals, banner-aware apply, assertion-weakening guard) accepted; PRD §9.6 expanded with §9.6.1 (MVP delivery); ADR README index updated. 0 AI co-author trailers. |
| 21 | done | PASS | ohswedd | 2026-05-29 | All ten tasks (21.01–21.10) shipped on branch `feature/phase-21-visual-regression-module`. `modules/visual` ships the Phase-21 visual-regression MVP (ADR-0026): `VisualModule(SentinelModule)` follows the standard module lifecycle and consumes PNGs already on disk (Playwright capture wire is a future TS-runtime extension per ADR-0026 follow-up obligations — not a Phase 21 deliverable). Diff math is pure-Python Pillow: `pixel_diff` (per-channel `ImageChops.difference` collapsed to a binary mask, plus a red-highlighted overlay PNG) + single-scale Wang et al. SSIM on the luminance channel (no Gaussian window — platform-stable). Perceptual filter is a noise filter only: a finding fires when BOTH the pixel threshold AND the SSIM threshold cross. Storage layout `.sentinel/baselines/<viewport>/<route-slug>.png` with `index.json` (sha256 + captured-at + captured-by-run-id + masks-applied per row, atomic writes via `*.tmp` → rename); run artifacts under `<run-dir>/visual/` (`current/` for captures, `diff/<viewport>/<route>.png` for overlays, `index.json` for per-pair status). Masking accepts `selector` (TS capture hides at screenshot time) OR `rect` (Python diff layer paints both images grey before comparison); wildcard `*` and prefix glob `admin*` both ship; selector-only masks are recorded but the capture layer is responsible for hiding. Three default viewports (`mobile 375×812`, `tablet 768×1024`, `desktop 1280×800`); name pattern `^[a-z0-9_-]+$` with uniqueness enforced. Three finding categories: `visual_pixel_diff` (medium), `visual_size_mismatch` (high — pixel-dimension drift is almost never noise), `visual_missing_current` (medium — baseline present, capture missing); `missing_baseline` is the operator's signal to accept, not a finding. CLI: `apps/cli/src/sentinel_cli/commands/visual_cmd.py` is a Typer subapp replacing the Phase-02 stub with `diff` (default — lifecycle-restricted), `accept`, and `capture` subcommands; exit codes 0 / 1 / 2 / 4 / 6 / 7; JSON mode supported; `--ci` honoured. Hard CI-acceptance guard (CLAUDE §29, §39): `sentinel visual accept` refuses to promote whenever `state.ci` is true OR `CI` / `SENTINEL_CI` is truthy in the env, exiting `4` (unsafe target) and writing a `visual.accept.refused_ci` audit-log entry under the supplied current root so operators have a paper trail. Config: `visual:` block extended with `viewports`, `masks`, `perceptual` blocks; `sentinel.config.yaml.example` documents the new surface; loader-side validation rejects duplicate viewport names, invalid name patterns, and masks missing both `selector` and `rect`. Pillow `11.0.0` added to engine + dev deps. Tests (98 new): 56 unit (skeleton 6 + baselines 11 + breakpoints 5 + diff 9 + findings 7 + helpers 7 + masking 9 + schema 8) + 17 integration module (diff 7 + masking 1 + breakpoints 2 + perceptual 2 + corrupt PNG 1 + summarize 3 + no-auto-accept 2) + 17 CLI integration (help + diff happy / change / JSON + accept CI refusal env + flag + local accept with audit log + filter mismatch + capture happy + capture missing + missing config x2 + URL override + corrupt index + JSON accept + JSON capture). `make ci` green: ruff format-check + lint + mypy strict (576 source files) + adr-check (26 ADRs incl. new ADR-0026) + pytest 2320 default + 1 skipped + Prettier + ESLint + tsc + Vitest 236 = 2556 total. `make coverage` 95.08 % (floor 95 %). `modules/visual` per-file: `__init__.py` / `baselines.py` / `breakpoints.py` / `masking.py` / `models.py` / `options.py` 100 %; `findings.py` 97 %; `diff.py` 95 %; `module.py` 92 % (package floor 85 %). `apps/cli/.../visual_cmd.py` 90 %. TS coverage unchanged at 87.44 % lines / 78.31 % branches (floors 85 / 75). ADR-0026 (Visual regression — Pillow-driven diff, signal-side capture, hard CI-acceptance guard) accepted; PRD §10.6 expanded with §10.6.1 (MVP delivery); `sentinel.config.yaml.example` documents the new visual viewports / masks / perceptual blocks. 0 AI co-author trailers. |
| 22 | done | PASS | ohswedd | 2026-05-29 | All ten tasks (22.01–22.10) shipped on branch `feature/phase-22-api-testing-module`. `modules/api` ships the Phase-22 API-testing MVP (ADR-0027): `ApiModule(SentinelModule)` follows the standard module lifecycle and drives every check via `httpx` (Python-side; no Playwright). Seven check kinds wired into `_RUN_ORDER`: `contract` (OpenAPI 3.x via `openapi-spec-validator` + `jsonschema` Draft 7; GraphQL via `graphql-core` SDL parse), `negative` (bounded variant catalogue — missing-required / wrong-type / out-of-range / oversized-string), `auth` (anonymous / expired-token sentinel / cross-user matrix), `latency` (skip-only — defers to Phase 12 perf module's `perf/api_latency` category; skip reason names both the perf category and the budget value), `pagination` (walks GETs with `page`/`cursor`/`offset` params; detects content-type drift, envelope drift, empty-page errors), `error_shape` (post-processes catalogues from prior checks; emits ERROR-SHAPE-DRIFT when an endpoint surfaces > 1 distinct rule-id in one run), `backward_compat` (diffs current `<run-dir>/api/api-schema.json` against `--diff-since <run-id>` or the alphabetically last sibling snapshot; emits REMOVED-ENDPOINT / REMOVED-REQUIRED-RESPONSE-FIELD / ADDED-REQUIRED-REQUEST-FIELD / CHANGED-RESPONSE-TYPE). **Layered no-fuzz guard (CLAUDE §30):** config schema clamps `api.negative_max_payload_kb` to `[1, 64]` KB and `api.negative_max_variants_per_endpoint` to `[1, 16]`; I/O-layer `safe_request` enforces `ABSOLUTE_MAX_REQUEST_BYTES = 64 KB` regardless of config (raises `RequestTooLargeError` before issuing); the negative-case generator iterates a fixed named catalogue, never a fuzz library; no CLI flag named `--aggressive` / `--fuzz` / `--brute` / `--stress` / `--unbounded` / `--no-rate-limit` exists; security guard `tests/security/test_api_no_aggressive_flags.py` greps the module + CLI source for those literals and introspects the Typer parameters for forbidden patterns. CLI: `apps/cli/src/sentinel_cli/commands/api_cmd.py` replaces the Phase-02 stub behind `sentinel api` with options `--url / --openapi / --graphql / --discovery / --diff-since / --checks`; exit codes 0 / 1 / 2 / 4 / 6 / 7; JSON mode emits a single machine-readable payload; audit log gets `api.cli.complete` entry per invocation. Config: new `api:` block (`enabled_checks` tuple, `openapi_path`, `graphql_path`, `graphql_endpoint`, `request_timeout_seconds`, `rate_per_second`, `negative_max_payload_kb`, `negative_max_variants_per_endpoint`, `auth_test_users` list, `pagination_max_pages`, `latency_min_samples`, `sample_endpoints_max`, `routes`); new `ApiAuthTestUser` block (`label`, `token_env`, `role`). Tests (132 new across 18 files): skeleton 10 + module internals 13 + openapi loader 10 + openapi edges 14 + graphql loader 6 + http_client 6 + negative unit 13 + pagination unit 7 + auth helpers 5 + check helpers 11 + backward-compat 11 + findings 4 = 110 unit; openapi-contract integration 5 + graphql-contract 6 + graphql-more 2 + negative integration 4 + negative oversize 1 + auth integration 4 + auth-more 2 + latency 1 + pagination/error-shape 4 + pagination drift 4 + module-e2e 4 + module-more 2 = 39 integration module; CLI integration 6; security guard 16. `tests/integration/cli/test_exit_codes.py` updated to reference `chaos` as its remaining stub. `make ci` green: ruff format-check + lint + mypy strict (623 source files) + adr-check (27 ADRs incl. new ADR-0027) + pytest 2492 default + 1 skipped + Prettier + ESLint + tsc + Vitest 236 = 2728 total. `make coverage` 95.04 % (floor 95 %). `modules/api` per-file: `__init__.py` / `checks/contract_graphql.py` / `checks/latency.py` / `http_client.py` / `models.py` / `options.py` 100 %; `checks/auth.py` 98 %; `module.py` 97 %; `graphql.py` 97 %; `findings.py` 95 %; `backward_compat.py` 94 %; `checks/contract_openapi.py` 94 %; `checks/negative.py` 92 %; `checks/pagination.py` 92 %; `openapi.py` 92 %; `checks/error_shape.py` 91 % (package floor 90 %). TS coverage unchanged at 87.44 % lines / 78.31 % branches (floors 85 / 75). ADR-0027 (API testing module — Python `httpx` with layered no-fuzz guards + perf-dedup latency) accepted; PRD §10.3 expanded with §10.3.1 (MVP delivery); ADR README index updated. 0 AI co-author trailers. |
| 23 | not started | — | — | — | — |
| 24 | not started | — | — | — | — |
| 25 | not started | — | — | — | — |
| 26 | not started | — | — | — | — |
| 27 | not started | — | — | — | — |
| 28 | not started | — | — | — | — |
| 29 | not started | — | — | — | — |

---

## PR & merge log

Every phase ends with a push → PR → CI → merge cycle handled by the loop prompt (see `PROMT.md` step 7). Record each phase's branch, PR, the CI run that turned the PR green, and the merge commit SHA on `main`. A phase row in the **Phase Gate Reviews** table above is not considered final until its row here is filled in.

| Phase | Branch | PR URL | Green CI run | Merge commit SHA | Merged on |
|---|---|---|---|---|---|
| 00 | feature/phase-00-foundation | (merged before PR & merge log existed) | — | — | 2026-05-27 |
| 01 | feature/phase-01-core-domain-config | PR #1 (Ohswedd/sentinelqa) | — | — | 2026-05-27 |
| 02 | feature/phase-02-cli-skeleton-run-lifecycle | — | — | — | 2026-05-27 |
| 03 | feature/phase-03-report-schemas-reporter | — | — | — | 2026-05-27 |
| 04 | feature/phase-04-typescript-playwright-runtime | https://github.com/Ohswedd/sentinelqa/pull/4 | https://github.com/Ohswedd/sentinelqa/actions/runs/26566004743 | bcce8b896af3c245dbbf4d92be131db2ce4f31f0 | 2026-05-28 |
| 05 | feature/phase-05-discovery-module | https://github.com/Ohswedd/sentinelqa/pull/6 | https://github.com/Ohswedd/sentinelqa/actions/runs/26574849155 | 2fde372595a40655fd282eda19a4d97f69556cd5 | 2026-05-28 |
| 06 | feature/phase-06-planner-module | https://github.com/Ohswedd/sentinelqa/pull/7 | https://github.com/Ohswedd/sentinelqa/actions/runs/26577068394 | 53f8720f59fb782da886bd73fc80a6a9864f3938 | 2026-05-28 |
| 07 | feature/phase-07-generator-module | https://github.com/Ohswedd/sentinelqa/pull/8 | https://github.com/Ohswedd/sentinelqa/actions/runs/26581099520 | 18a6a66a75e029eb9894ef11867e50be94572cc9 | 2026-05-28 |
| 08 | feature/phase-08-runner-module | https://github.com/Ohswedd/sentinelqa/pull/9 | https://github.com/Ohswedd/sentinelqa/actions/runs/26584149975 | 3909ccf9dfa8130ca747def230b513dca40f891c | 2026-05-28 |
| 09 | feature/phase-09-analyzer-module | https://github.com/Ohswedd/sentinelqa/pull/10 | https://github.com/Ohswedd/sentinelqa/actions/runs/26586376163 | d368da9897a75d7553e9475cb601368df4cce4ca | 2026-05-28 |
| 10 | feature/phase-10-functional-module | https://github.com/Ohswedd/sentinelqa/pull/11 | https://github.com/Ohswedd/sentinelqa/actions/runs/26589112750 | a7303ccab1f8132cbcb55036d88c2909db0b7fb7 | 2026-05-28 |
| 11 | feature/phase-11-accessibility-module | https://github.com/Ohswedd/sentinelqa/pull/12 | https://github.com/Ohswedd/sentinelqa/actions/runs/26591342678 | d946d83bd0faf2cb73495ec10c3ee428bdcee632 | 2026-05-28 |
| 12 | feature/phase-12-performance-module | https://github.com/Ohswedd/sentinelqa/pull/13 | https://github.com/Ohswedd/sentinelqa/actions/runs/26593900242 | 264caccc9d8bfebe08adba4dc2ac1890d060502c | 2026-05-28 |
| 13 | feature/phase-13-security-safe-module | https://github.com/Ohswedd/sentinelqa/pull/14 | https://github.com/Ohswedd/sentinelqa/actions/runs/26598741657 | 012e0dd45d49c09da1133780872c0a2a0c0e63e1 | 2026-05-28 |
| 14 | feature/phase-14-quality-scoring | https://github.com/Ohswedd/sentinelqa/pull/15 | https://github.com/Ohswedd/sentinelqa/actions/runs/26611071661 | c41576a06ffd46a01b9ef9db14a859fe582fd076 | 2026-05-29 |
| 15 | feature/phase-15-html-json-reports | https://github.com/Ohswedd/sentinelqa/pull/16 | https://github.com/Ohswedd/sentinelqa/actions/runs/26612521821 | 77a58fd30f4c4fb2620a58e29119a8ae06b2a8f6 | 2026-05-29 |
| 16 | feature/phase-16-python-sdk | https://github.com/Ohswedd/sentinelqa/pull/17 | https://github.com/Ohswedd/sentinelqa/actions/runs/26635410681 | 6fe90fc762c15f7dfb31cdd4d7649e61cff423cd | 2026-05-29 |
| 17 | feature/phase-17-ci-integration | https://github.com/Ohswedd/sentinelqa/pull/18 | https://github.com/Ohswedd/sentinelqa/actions/runs/26639224755 | 3f44fc4b2f24b9a901abd2d0d5192c9a79c45082 | 2026-05-29 |
| 18 | feature/phase-18-mcp-agent-interface | https://github.com/Ohswedd/sentinelqa/pull/19 | https://github.com/Ohswedd/sentinelqa/actions/runs/26642415840 | 5eaa5e0a30933e762180ec14e86556d0e3d21eed | 2026-05-29 |
| 19 | feature/phase-19-llm-code-audit-module | https://github.com/Ohswedd/sentinelqa/pull/20 | https://github.com/Ohswedd/sentinelqa/actions/runs/26652890472 | 540d7135f289392362205cb85fd9e589d3e337e5 | 2026-05-29 |
| 20 | feature/phase-20-healer-self-repair | https://github.com/Ohswedd/sentinelqa/pull/21 | https://github.com/Ohswedd/sentinelqa/actions/runs/26655437322 | bbeb36139d34a0a3f650b96c94acf83e6969b692 | 2026-05-29 |
| 21 | feature/phase-21-visual-regression-module | https://github.com/Ohswedd/sentinelqa/pull/22 | https://github.com/Ohswedd/sentinelqa/actions/runs/26657270170 | 139f2cf861de5aba6862ad03b6dd1ecc65f01557 | 2026-05-29 |
| 22 | feature/phase-22-api-testing-module | https://github.com/Ohswedd/sentinelqa/pull/23 | https://github.com/Ohswedd/sentinelqa/actions/runs/26661013662 | 6ee1b24c669a6b1da7a28a88cb0e30fcf1467303 | 2026-05-29 |
| 23 | — | — | — | — | — |
| 24 | — | — | — | — | — |
| 25 | — | — | — | — | — |
| 26 | — | — | — | — | — |
| 27 | — | — | — | — | — |
| 28 | — | — | — | — | — |
| 29 | — | — | — | — | — |

> **Rule change effective 2026-05-28:** the loop prompt now requires push → CI-watch → merge on `main` as part of closing every phase. Phase 04 was retroactively closed under the new rule: PR #4 was merged on 2026-05-28 (squash, branch deleted) and this table backfilled on the same day. From Phase 05 onward the close-out happens inside the phase loop and no row should ever sit in `_pending_`.

---

## Deferred-scope register

This table must remain empty at every phase boundary. If a phase needs to defer something, it must be planned into a future phase and added to that phase's task list **before** the gate review passes. Items here at gate time block the phase from closing.

| Date | Phase | Item | Why deferred | Re-homed to phase | Resolved? |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

---

## PRD / CLAUDE.md sync log

Whenever a phase changes behavior, schemas, or boundaries, record the doc update here.

| Date | Phase | Doc updated | Section(s) | Commit |
|---|---|---|---|---|
| 2026-05-27 | 00 | _(none)_ | Phase 00 introduced no behavior changes, no schema changes, no safety-boundary changes, no scoring changes, and no CLI/SDK contract changes. PRD.md and CLAUDE.md were both untouched (PRD trailing whitespace was inadvertently rewritten by the pre-commit hook during a probe and immediately restored; PRD.md, CLAUDE.md were added to the trailing-whitespace + end-of-file-fixer exclude list to make the protection permanent). No sync entry required. | n/a |
| 2026-05-27 | 01 | PRD.md | §13.2 Exit codes — replaced 7-code informal grid with the canonical 8-code grid from CLAUDE.md §13; now lists the Phase 01 exception type bound to each code so `engine/errors/codes.py` is the single source of truth. Conflict resolution per CLAUDE §2 authority order (CLAUDE > PRD). | a5ab2d1 |
| 2026-05-27 | 01 | _(none beyond §13.2)_ | Phase 01 introduces typed core models, the strict config loader, the safety policy, the redaction layer, and the structured logger. None of those changes alter externally-observable product behavior beyond the exit-code grid logged above. CLI/SDK contracts have not shipped yet (Phase 02+), so the PRD's §13.1 CLI and §14 SDK sections did not need updating. | (this branch) |
| 2026-05-27 | 02 | _(none)_ | Phase 02 implements behavior PRD §10–§13, §17, and §26 already specify in full. The CLI now exists at `apps/cli/src/sentinel_cli/`, but every command listed in PRD §13.1 was already promised — three are implemented (`init`, `doctor`, `audit`), the other 16 remain phase-stubbed per PRD §32 (recommended build order) and CLAUDE.md §37 (no fake completion). The 17-step run lifecycle codified in `engine/orchestrator/run_lifecycle.py` matches CLAUDE.md §10 step-for-step. The new `RunStatus="dry_run"` value is an additive enum expansion in `engine.domain.test_run` and `packages/shared-schema/schemas/test_run.schema.json`; PRD §18.1 lists entities by name only (no enum surface), so no PRD §18 text changed. JSON-mode purity (CLAUDE §13) is now enforced and tested. Exit-code grid behavior is unchanged from the Phase-01 row above — Phase 02 only wires the runtime mapping (`failed`→1, `unsafe_blocked`→4, `incomplete`→6, `dry_run`→0). | (this branch) |
| 2026-05-27 | 03 | PRD.md | §20 Evidence and Reporting Requirements — added §20.1 (Persisted artifacts table mapping every Phase-03 artifact to its schema + writer), §20.2 (Finding evidence requirement; critical/high/medium without evidence is rejected at write time), §20.3 (Schema drift guard: meta-schema check + byte-locked goldens + hypothesis property tests). The persisted shapes for `run.json`/`findings.json`/`score.json` are new wire formats (not domain models) and are versioned via `schema_version` at the root; ADR-0008 owns the rationale. CLI/SDK contracts not changed beyond the existing `config.report.formats` literal. | (this branch) |
| 2026-05-28 | 04 | PRD.md | §15 TypeScript Runtime Specification — expanded §15.1 (purpose + subpath exports), kept §15.2 (generated-test example now uses `@sentinelqa/ts-runtime/playwright` and the `sentinel` fixture), added §15.3 (sentinel-ts binary contract + exit codes), §15.4 (JSONL event protocol with all 14 kinds + ts-events schema + ADR-0009 link), §15.5 (evidence capture defaults aligned to CLAUDE §21 + AX-tree hash for the Healer), §15.6 (semantic locator strategy chain consumed by Phase 07 / 20), §15.7 (safety boundary + Python-authoritative redaction with byte-parity fixture). New wire format (`ts-events.schema.json` v1.0.0) is locked by ADR-0009; CLI/SDK contracts in §13 / §14 are unchanged. | (this branch) |
| 2026-05-28 | 05 | PRD.md | §9.1 Discovery module — added §9.1.1 (MVP delivery): names every shipped class in `engine.discovery`, documents the deterministic risk-rule set, lists the five JSON artifacts + Markdown summary `sentinel discover` writes, restates the safety boundary (env-var-only credentials, login body never persisted, allowlist enforced before I/O), reserves `discovery.engine: playwright` for Phase 17. ADR-0010 owns the rationale. | (this branch) |
| 2026-05-28 | 05 | CLAUDE.md | _(no edit)_ — Phase 05 adds a module package, an ADR, and a CLI command; it doesn't change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-28 | 06 | PRD.md | §9.2 Planner — added §9.2.1 (MVP delivery): names every shipped class in `engine.planner`, documents the deterministic rule set, lists every built-in flow extractor, restates the optional LLM adapter, USD budget enforcement, locked-prompt versioning, vendor-neutral HTTP-only provider design, safety boundary (env-var credentials, graph summary excludes form values / headers / cookies), and the `sentinel plan` CLI contract. ADR-0011 owns the rationale. New wire format (`plan.schema.json` v1) is locked under ADR-0011. CLI / SDK contracts in §13 / §14 are unchanged. | 53f8720 |
| 2026-05-28 | 06 | CLAUDE.md | _(no edit)_ — Phase 06 adds a module package, an ADR, a CLI command, two domain-model field additions (`Flow.description/confidence/tags/extractor/source`, `TestCase.module`), and a `planner.llm` config block. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-28 | 07 | PRD.md | §9.3 Generator — added §9.3.1 (MVP delivery): names every shipped class in `engine.generator`, the 14 templates, the page-object emission thresholds, the auth/data/setup fixture set with the `authorized_destructive`-only data-seed gate, the brittleness-audit pipeline backed by the new `sentinel-ts audit-locators` subcommand, the writer's banner-aware hand-edit semantics, the `sentinel generate` CLI contract with full exit-code grid, the determinism contract (filenames derived from `extractor + flow.name` + discovery-ID disambiguator; never planner-generated IDs), and the safety boundary stance. ADR-0012 owns the rationale. CLI/SDK contracts in §13/§14 are unchanged. | 18a6a66 |
| 2026-05-28 | 07 | CLAUDE.md | _(no edit)_ — Phase 07 adds a module package, an ADR, a CLI command, and a new TS CLI subcommand. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-28 | 08 | PRD.md | §9.4 Runner — added §9.4.1 (MVP delivery): documents `engine.runner` (LocalRunner + DockerRunner sharing the `RunnerInvocation → RunnerOutcome` contract), the JSONL aggregator with partial-stream tolerance and pass-on-retry→`flaky`, the strict quarantine list with `expires_at` cap and `http(s)://` `issue_url` guard, deterministic sharding via SHA-1 of POSIX paths with `merge_outcomes` worst-of status, the pinned `mcr.microsoft.com/playwright:v1.49.0-jammy` Docker image with safe mount defaults, and the `sentinel test` CLI contract with full exit-code grid. New wire format (`module-results/<module>.json` v1, `runner` config block) is locked under ADR-0013. CLI / SDK contracts in §13 / §14 are unchanged. | (this branch) |
| 2026-05-28 | 08 | CLAUDE.md | _(no edit)_ — Phase 08 adds a module package, an ADR, a CLI command, and a config block. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-28 | 09 | PRD.md | §9.5 Analyzer — added §9.5.1 (MVP delivery): documents `engine.analyzer` (categorize → root_cause → repro → retry_decision → optional LLM refinement) with each class / function and its safety contract (credential-free repro per CLAUDE §33, two-retry cap per CLAUDE §23, LLM never replaces deterministic output). Names the rehome of `RunLifecycle.run_modules`'s broad `except Exception` (CLAUDE §10) through `categorize_module_error` to populate `ModuleOutcome.error_category` / `_confidence` / `_rationale` / `_type`. New config block (`analyzer.llm`) and locked prompt (`engine/analyzer/llm_prompts/explainer.v1.md`, PROMPT_VERSION=1) are locked under ADR-0014. CLI / SDK contracts in §13 / §14 are unchanged. | d368da9 |
| 2026-05-28 | 09 | CLAUDE.md | _(no edit)_ — Phase 09 adds a module package, an ADR, a config block, and rehomes a previously-deferred broad-except. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-28 | 10 | PRD.md | §10.1 Functional E2E testing — added §10.1.1 (MVP delivery): names `modules/functional/FunctionalModule` and the seven-step `SentinelModule` lifecycle from `engine.modules.base`, the findings translation contract (`build_finding_from_failed_test` + PRD §20 evidence fallback to the per-module runner log), the canonical `@p0..p3 / @module:<n> / @flow:<extractor> / @risk:<level>` tag set emitted on every spec, the slice modes (`smoke / standard / full`) resolved through `TagSelection`, the `sentinel functional` CLI contract (options + exit-code grid), the new `module_options` lifecycle channel + `RunLifecycle.last_context` accessor, and the payment-sandbox safety contract (Stripe `4242` test card, `SENTINEL_PAYMENT_SANDBOX=1` env gate, env-var credentials only — CLAUDE §33). ADR-0015 owns the rationale. CLI / SDK contracts in §13 / §14 are unchanged beyond the `sentinel functional` command landing. | a7303cc |
| 2026-05-28 | 10 | CLAUDE.md | _(no edit)_ — Phase 10 adds a module package, an ADR, a CLI command, a new optional `grep` field on the run-config schema (Python + TS in parity), and per-module options threading on the lifecycle. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-28 | 11 | PRD.md | §10.4 Accessibility testing — added §10.4.1 (MVP delivery): names `modules.accessibility.AccessibilityModule(SentinelModule)` and the seven-step `SentinelModule` lifecycle, the dedicated `A11yRunner` Protocol + `LocalA11yRunner` subprocess strategy, the new `sentinel-ts audit-a11y` subcommand contract (input config schema, output artifacts under `<run-dir>/a11y/`), the per-route + per-check rule set with explicit "placeholder is NOT a sufficient accessible name" clause, the severity / confidence mapping (`critical|serious→high`, `moderate→medium`, `minor→low`; `0.95` stable / `0.6` experimental / `0.9` deterministic checks), the wire-format schema_version pin under ADR-0016 §3, the `sentinel a11y` CLI contract + exit-code grid (0/1/2/4/5/6), the CLAUDE §28 wording guard, and the explicit `pnpm add axe-core` requirement for projects that adopt the module. ADR-0016 owns the rationale. CLI / SDK contracts in §13 / §14 are unchanged beyond the `sentinel a11y` command landing. | d946d83 |
| 2026-05-28 | 11 | CLAUDE.md | _(no edit)_ — Phase 11 adds a module package, an ADR, a CLI command, a new TS subcommand, an `accessibility` config block, and the runner-abstraction pattern (`A11yRunner` Protocol parallels but does not change the CLAUDE §9 module contract). None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-28 | 12 | PRD.md | §10.5 Performance testing — added §10.5.1 (MVP delivery): names `modules.performance.PerformanceModule(SentinelModule)` and the seven-step `SentinelModule` lifecycle from `engine.modules.base`, the dedicated `PerformanceRunner` Protocol + `LocalPerformanceRunner` subprocess strategy, the new `sentinel-ts audit-perf` subcommand contract (input config schema, output artifacts under `<run-dir>/perf/`), the per-check rule set (page budgets / API P95 / bundle / CPU / nav stability) with severity policy (page+CPU+bundle overage >50% → high else medium; API P95 overage >100% → high else medium; nav stability is `low` / confidence `0.5`), the wire-format schema_version pin under ADR-0017 §5, the `sentinel perf` CLI contract + exit-code grid (0/1/2/4/5/6), the CLAUDE §27 synthetic-labelling guard, and the no-new-dependency property (Playwright dynamic-imported by the launcher). ADR-0017 owns the rationale. CLI / SDK contracts in §13 / §14 are unchanged beyond the `sentinel perf` command landing. | 264cacc |
| 2026-05-28 | 12 | CLAUDE.md | _(no edit)_ — Phase 12 adds a module package, an ADR, a CLI command, a new TS subcommand, an expanded `performance` config block, and reuses the runner-abstraction pattern introduced in ADR-0016 (no project-wide engineering-rule change). CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-28 | 13 | PRD.md | §10.7 Security testing — added §10.7.1 (MVP delivery): names `modules.security.SecurityModule(SentinelModule)` and the seven-step CLAUDE §9 lifecycle, the shared `CheckContext`, the eleven per-check files under `modules/security/checks/`, the gated-probe contracts (stored XSS / SQLi / SAST), the stable `SEC-*` rule catalog + SARIF registration, the wire-format `SECURITY_RESULT_SCHEMA_VERSION="1"` pin, the safety-contract guard tests (`test_module_calls_policy.py` + `test_security_forbidden_flags.py`), the audit-logging discipline, the `sentinel security` CLI contract + exit-code grid (0/1/2/4/5/6), and the new config surface (`security.checks`, `security.dependency_scanners`, `security.routes`, `security.request_timeout_seconds`, `auth.second_user`). ADR-0018 owns the rationale. CLI / SDK contracts in §13 / §14 are unchanged beyond the `sentinel security` command landing. | 012e0dd |
| 2026-05-28 | 13 | CLAUDE.md | _(no edit)_ — Phase 13 adds a module package, an ADR, a CLI command, two new config blocks, and an `auth.second_user` block. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-29 | 14 | PRD.md | §19 Quality Scoring — added §19.5 (MVP delivery): names every shipped piece in `engine.scoring` (`compute_score`, `compute_blockers`, `decide`, `register_scoring_hooks`), the eight PRD §19.1 axes + default weights, the flake-risk derivation from `ModuleResult.metrics["flake_rate"]`, the severity-penalty defaults (PRD §19.2 midpoints) + the new `policy.severity_penalty_high/medium/low` config knobs (10..25 / 3..10 / 1..3 ranges), the fixed critical penalty of 30, the typed `PolicyDecision` decision priority, the `sentinel report --explain-score` CLI contract + Phase-15-stub-replacement scope, and the reproducibility guard (hypothesis 5000-example property test + three byte-locked canonical replay goldens under `tests/integration/scoring/expected/`). New wire-format usage (no schema change: `score.schema.json` was already locked under ADR-0008; the new severity_penalty_* fields live on `PolicyConfig` and only surface through `config.snapshot.yaml`). ADR-0019 owns the rationale. CLI / SDK contracts in §13 / §14 are unchanged beyond the `sentinel report --explain-score` command landing. | c41576a |
| 2026-05-29 | 14 | CLAUDE.md | _(no edit)_ — Phase 14 adds a scoring package, an ADR, a CLI command, three new `policy.severity_penalty_*` config fields, and the per-hook sentinel-flag cleanup on `ModuleRegistry.clear()`. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-29 | 15 | PRD.md | §9.7 Reporter module — added §9.7.1 (MVP delivery): names every shipped piece in Phase 15 — the HTML writer + bundled template / CSS / JS + offline + a11y guarantees, the PR-comment upsert anchor + character cap, the trends derivation rules, the audit-trail view, the Slack Block Kit payload (generated only), and the new `sentinel report` CLI surface (`--latest / --run-id / --format / --open / --explain-score`). New wire-format additions: `HTML_REPORT_SCHEMA_VERSION="1"` (HTML template) and the vendored Slack Block Kit subset schema at `packages/shared-schema/external/slack-block-kit.schema.json`. Both are locked under ADR-0020. CLI/SDK contracts in §13/§14 are unchanged beyond the `sentinel report` re-render surface landing. | 77a58fd |
| 2026-05-29 | 15 | CLAUDE.md | _(no edit)_ — Phase 15 adds reporter modules, an ADR, a CLI command surface, a vendored Slack Block Kit subset schema, and one new gitleaks allowlist path (audit-view test asserts redacter strips a fake Stripe-style key from the persisted audit log; the literal is required to assert the negative). None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-29 | 16 | PRD.md | §14 Python SDK Specification — added §14.5 (MVP delivery): names the shipped `Sentinel` facade with every method + its `async_<name>` counterpart, the constructor surface (`project_path`, `config`, `machine_readable`, `artifacts_root`), the public model surface (`AuditResult` with derived views + `to_agent_messages()`; `QualityGate.from_config` and `Policy.from_config` views), the agent-message contract (every public exception, `Finding`, `RepairSuggestion`, `AuditResult` all expose stable redacted dicts versioned by `AGENT_MESSAGE_SCHEMA_VERSION`; `sentinelqa.agent.format` deterministic serializer), the error round-trip via `sentinelqa.errors.from_dict`, the lazy-import contract (`import sentinelqa` measured ~80 ms vs 200 ms budget), the public surface gate (`packages/python-sdk/api-snapshot.json` + `make sdk-api-snapshot` + the unit-test diff), the `_internal/` non-public boundary, and the `verify_fix` deferred-until-Phase-20 contract (CLAUDE.md §37). ADR-0021 owns the rationale. CLI contract in §13 unchanged. | 6fe90fc |
| 2026-05-29 | 16 | CLAUDE.md | _(no edit)_ — Phase 16 adds an SDK package, an ADR, an API snapshot gate, a deprecation-policy doc, two `to_agent_message()` methods on the existing `Finding` / `RepairSuggestion` domain entities, and a `make sdk-api-snapshot` target. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-29 | 17 | PRD.md | §9.1 Discovery — added §9.1.2 (Playwright backend delivery, Phase 17 task 07): names the TS `sentinel-ts discover` subcommand, the two new JSONL event kinds (`discovery.page`, `discovery.endpoint`), the Python `engine.discovery.backends.playwright_backend.PlaywrightCrawlBackend` (`CrawlBackend` Protocol implementation with one-to-one event-to-CrawlPage translation), `discovery.engine: "playwright"` going live (no longer reserved), the new `discovery-playwright (gated)` CI lane, the backend-parity test, and ADR-0010 follow-up #1 resolution. §15.3 sentinel-ts command table extended with `audit-locators`, `audit-a11y`, `audit-perf`, and `discover` rows. §15.4 documents the sixteen event kinds (was fourteen, now adds the two `discovery.*` events). §21.4 (MVP delivery, Phase 17) names `engine/ci/modes.apply_mode`, the diff-aware heuristics + tripwires, the `sentinel ci` CLI surface + ci.json sidecar, the GitHub composite Action + reusable workflow, the GitLab template, the PR/MR posters, and the safety contract. New wire format usage: `discovery.page` / `discovery.endpoint` events locked under ADR-0010 follow-up; `ci.json` sidecar locked under ADR-0022. CLI / SDK contracts in §13 / §14 are unchanged beyond the `sentinel ci` command landing. | 3f44fc4 |
| 2026-05-29 | 17 | CLAUDE.md | _(no edit)_ — Phase 17 adds the CI integration core, two GitHub/GitLab adapters, the PR/MR posters, two ADRs (0010 follow-up annotation + new 0022), a `discovery.engine: "playwright"` config-key activation, two new JSONL event kinds, and a new CI lane. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-29 | 18 | PRD.md | §16 MCP / LLM Tool Interface — added §16.4 (MVP delivery): names the shipped `packages/mcp-server/` package, the wire protocol (JSON-RPC 2.0 / NDJSON-framed stdio at MCP protocol version `2024-11-05`), every PRD §16.1 tool (plus `ping`), the `AgentEnvelope` shape locked at `packages/shared-schema/agent-envelope.schema.json`, the safety contract enforced via `sentinelqa_mcp.tools._safety.enforce_url` + the AST guard at `tests/security/test_mcp_safety.py`, the `sentinel.verify_fix` four-valued decision matrix (`fix_verified` / `partial` / `regressed` / `still_failing`), the `sentinel mcp` CLI options + exit-code grid, the `sentinel.read_report` path-safety + truncation rules, and the stderr-only logging contract (CLAUDE.md §13). New wire format (`AGENT_ENVELOPE_SCHEMA_VERSION="1"`) locked under ADR-0023. CLI contract in §13 expanded (the `sentinel mcp` stub becomes a live command). SDK contract in §14 unchanged beyond the in-process integration the MCP tools rely on. | 5eaa5e0 |
| 2026-05-29 | 18 | CLAUDE.md | _(no edit)_ — Phase 18 adds the `packages/mcp-server` package, an ADR, a CLI command, a vendored Draft 2020-12 schema (`agent-envelope.schema.json`), two new pyproject filterwarnings (`ResourceWarning`, `PytestUnraisableExceptionWarning`), and a Claude Desktop example. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-29 | 19 | PRD.md | §10.9 LLM-code audits — added §10.9.1 (MVP delivery): names the shipped `modules.llm_audit.LlmAuditModule(SentinelModule)`, the signal-driven contract (Phase 05 discovery output + optional runner artifacts + optional source root), every shipped check + its stable `LLM-*` rule ID + severity / confidence default, the `LlmAuditInputs` typed signal envelope, the hardcoded-credential double-redaction policy (CLAUDE.md §33), the module's status policy (`skipped` / `passed` / `failed`), the `sentinel llm-audit` CLI surface + exit-code grid (0 / 1 / 2 / 4 / 6 / 7), the `<run-dir>/llm_audit/index.json` per-run summary artifact, and the dedicated HTML / PR-comment "LLM-Code Audit" report section. ADR-0024 owns the rationale; ADR-0023's `verify_fix` Phase-20 follow-up remains the Healer's apply-fix work. CLI / SDK contracts in §13 / §14 are unchanged beyond the `sentinel llm-audit` command landing. | 540d713 |
| 2026-05-29 | 19 | CLAUDE.md | _(no edit)_ — Phase 19 adds a module package, an ADR, a CLI command, a `.gitleaks.toml` allowlist for the new test fixtures, and a fixture catalogue. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-29 | 20 | PRD.md | §9.6 Healer module — added §9.6.1 (MVP delivery): names every shipped piece in `engine/healer/` (three deterministic proposers with confidence tiers, the `assert_no_assertion_weakening` guard, `detect_banner_status` hand-edit detection, `decide_auto_apply` gating, atomic JSON writer, `Healer.propose` facade), the new `RepairProposal` wire format locked at `packages/shared-schema/repair-proposal.schema.json` (Draft 2020-12, schema_version "1"), the Analyzer ↔ Healer routing helper `is_healer_candidate`, the `sentinel fix` CLI surface and exit-code grid (0 / 2 / 6), the MCP `sentinel.suggest_fix` healer-proposal surfacing, and the new `healer:` config block. ADR-0025 owns the rationale. CLI / SDK contracts in §13 / §14 unchanged beyond `sentinel fix` landing. | bbeb361 |
| 2026-05-29 | 20 | CLAUDE.md | _(no edit)_ — Phase 20 adds a module package, an ADR, a CLI command, a new top-level wire schema (`repair-proposal.schema.json`), a `healer:` config block, and a coverage source addition for `engine/healer`. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-29 | 21 | PRD.md | §10.6 Visual testing — added §10.6.1 (MVP delivery): names the shipped module (`modules/visual.VisualModule`), the Pillow-based diff math (`pixel_diff` + single-scale Wang et al. SSIM perceptual filter), the on-disk storage layout (`.sentinel/baselines/<viewport>/<route-slug>.png` + `index.json` with sha256 / captured-at / captured-by-run-id / masks-applied per row), the masking contract (`selector` vs `rect`, wildcard `*`, prefix glob `admin*`), the three default viewports (mobile / tablet / desktop), the `sentinel visual` Typer subapp (`diff` / `accept` / `capture`), the hard CI-acceptance guard (CLAUDE §29, §39 — exit 4 + `visual.accept.refused_ci` audit log), the three finding categories (`visual_pixel_diff` medium, `visual_size_mismatch` high, `visual_missing_current` medium), and the exit-code grid (0 / 1 / 2 / 4 / 6 / 7). ADR-0026 owns the rationale. CLI / SDK contracts in §13 / §14 unchanged beyond `sentinel visual` landing. | 139f2cf |
| 2026-05-29 | 21 | CLAUDE.md | _(no edit)_ — Phase 21 adds a module package, an ADR, a Typer subapp, three new config blocks (`viewports`, `masks`, `perceptual`), a Pillow runtime dep, and a coverage source addition for `modules/visual`. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
| 2026-05-29 | 22 | PRD.md | §10.3 API testing — added §10.3.1 (MVP delivery): names every shipped check in `modules/api/` with its stable rule-id catalogue (CONTRACT-*, GRAPHQL-*, NEGATIVE-*, AUTH-*, PAGINATION-*, ERROR-SHAPE-*, COMPAT-*), the layered no-fuzz guard (config clamps + I/O `ABSOLUTE_MAX_REQUEST_BYTES = 64 KB` cap + fixed variant catalogue + no aggressive CLI flag + `tests/security/test_api_no_aggressive_flags.py` guard), the latency-dedup contract with the Phase 12 perf module (`perf/api_latency`), the backward-compat snapshot location (`<run-dir>/api/api-schema.json`) and resolution order (`--diff-since` then alphabetically last sibling run-dir), the deferral of GraphQL subscription probing to Phase 23 (chaos / session lifecycle), and the persisted artifact tree under `<run-dir>/api/`. ADR-0027 owns the rationale. CLI / SDK contracts in §13 / §14 unchanged beyond `sentinel api` landing. | (this branch) |
| 2026-05-29 | 22 | CLAUDE.md | _(no edit)_ — Phase 22 adds a module package, an ADR, a Typer command replacing the Phase-02 stub, two new config blocks (`api`, `ApiAuthTestUser`), and a coverage source addition for `modules/api`. None of those change a project-wide engineering rule, so CLAUDE.md is untouched. Sync log entry intentionally records the no-op. | n/a |
