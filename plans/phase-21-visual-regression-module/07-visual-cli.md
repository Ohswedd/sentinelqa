# Task 21.07 — `sentinel visual` command

## Deliverables

- Replace Phase 02 stub of `visual`.
- Options: `--url`, `--config`, `--viewports`, `--accept-baselines`, `--threshold`, `--ci`, `--json`.
- Outputs `visual/` artifacts (baselines, diffs, before/after PNGs).

## Tests required

- `tests/integration/cli/test_visual.py`.

## PRD / CLAUDE.md references

- PRD §13, §10.6.
- CLAUDE.md §13, §29.

## Definition of Done

- [ ] CLI command working.
- [ ] `STATUS.md` updated.
