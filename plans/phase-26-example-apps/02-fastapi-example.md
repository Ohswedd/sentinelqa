# Task 26.02 — FastAPI example

## Deliverables

- `examples/fastapi/` containing a typed FastAPI app exposing the same "Project" CRUD endpoints with OpenAPI auto-generation.
- `examples/fastapi/openapi.json` checked in for tests; regenerated by `make demo:fastapi:openapi`.
- Configurable to act as a backend for the Next.js example.

## Acceptance criteria

- `make demo:fastapi` boots on port 8000.
- `sentinel api --url http://localhost:8000 --openapi /openapi.json` produces zero critical findings.

## Tests required

- `tests/integration/examples/test_fastapi.py`.

## Definition of Done

- [ ] App + tests.
- [ ] `STATUS.md` updated.
