# Task 11.06 — `sentinel a11y` command

## Deliverables

- Replace Phase 02 stub of `a11y`.
- Options: `--url`, `--config`, `--routes` (comma-separated subset), `--axe-tags`, `--ci`, `--json`.
- End-to-end run produces `a11y/` artifacts + findings + module result.

## Acceptance criteria

- Works against fixture; CLI tests cover happy + failing paths.

## Tests required

- `tests/integration/cli/test_a11y.py`.

## PRD / CLAUDE.md references

- PRD §13.
- CLAUDE.md §13.

## Definition of Done

- [ ] CLI command implemented and tested.
- [ ] `STATUS.md` updated.
