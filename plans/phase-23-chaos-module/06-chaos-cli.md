# Task 23.06 — `sentinel chaos` command

## Deliverables

- Replace Phase 02 stub of `chaos`.
- Options: `--url`, `--scenarios <list>`, `--flows <list>`, `--ci`, `--json`.
- Default scenarios: network, session, ux, data. All scoped to local/staging targets.

## Tests required

- `tests/integration/cli/test_chaos.py`.

## Definition of Done

- [ ] CLI command working.
- [ ] `STATUS.md` updated.
