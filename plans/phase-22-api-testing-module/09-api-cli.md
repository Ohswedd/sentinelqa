# Task 22.09 — `sentinel api` command

## Deliverables

- Replace Phase 02 stub of `api`.
- Options: `--url`, `--openapi`, `--graphql`, `--negative`, `--auth`, `--latency`, `--diff-since <run-id>`, `--ci`, `--json`.
- Aggressive fuzzing flag is **not** implemented (forbidden in MVP).

## Tests required

- `tests/integration/cli/test_api.py`.

## PRD / CLAUDE.md references

- PRD §13, §10.3.
- CLAUDE.md §13, §30.

## Definition of Done

- [ ] CLI command working.
- [ ] `STATUS.md` updated.
