/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // poweredByHeader intentionally left ON — the realistic AI-generated app surface.
};

export default nextConfig;
