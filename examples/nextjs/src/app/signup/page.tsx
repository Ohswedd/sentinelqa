export default function SignupPage() {
  return (
    <section aria-labelledby="signup-heading">
      <h2 id="signup-heading">Sign up</h2>
      <p>
        Self-service sign-up is disabled in this demo. Use the credentials documented in
        <code> README.md</code> to log in.
      </p>
    </section>
  );
}
