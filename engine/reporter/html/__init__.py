"""Phase 15 HTML report assets.

The Jinja2 template, CSS, and JavaScript are loaded as resources by
:mod:`engine.reporter.html_writer`. Keeping them in a package directory
makes them importable + bundled into wheels without ad-hoc data files.
"""
