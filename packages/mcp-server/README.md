# @sentinelqa/mcp-server

Status: `Planned` — MCP server exposing the twelve `sentinel.*` tools (PRD §16) to LLM coding agents. Real implementation lands in Phase 18 (`plans/phase-18-mcp-agent-interface/`).

Placeholder runtime is **TypeScript** (Node 20+) to align with the canonical Anthropic MCP TS SDK; Phase 18 will confirm or revise that choice in an ADR. Currently empty — only a placeholder `src/index.ts` keeps the workspace whole.
