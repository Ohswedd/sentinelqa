"""SentinelQA Python SDK — placeholder package.

The public SDK lands in Phase 16 (`plans/phase-16-python-sdk/`). This module
intentionally exposes nothing so importing it cannot accidentally suggest
features that do not exist (CLAUDE.md §37: no fake completion).
"""

__all__: list[str] = []
