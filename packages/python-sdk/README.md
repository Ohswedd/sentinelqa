# sentinelqa (Python SDK)

Status: `Planned` — the public SDK surface (`Sentinel`, `AuditResult`, `Finding`, `TestPlan`, async support) lands in Phase 16. This package currently exposes only the empty `sentinel` namespace so the workspace, type-checker, and import graph are in place.

See PRD §14 and the Phase 16 task files under `plans/phase-16-python-sdk/`.
