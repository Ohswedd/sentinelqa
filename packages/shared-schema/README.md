# @sentinelqa/shared-schema

Status: `Planned` — JSON Schema sources for SentinelQA's serialized outputs (our product spec, §20). Schemas land in Phase 03 ().

This package is **TypeScript** because the schemas are authored as TS objects (and emitted to JSON) so the Python and TS sides share a single source. Currently empty — only a placeholder `src/index.ts` keeps the workspace whole.
