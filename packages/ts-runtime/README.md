# @sentinelqa/ts-runtime

Status: `Planned` — Playwright helpers, locator utilities, runtime tracing, JSONL bridge (PRD §15). Real surface lands in Phase 04.

This package currently exports nothing — only a placeholder `src/index.ts` and a smoke test so the workspace, type-checker, and Vitest harness are real now.
